# Vol 3 · The Compiler — Phase-A OUTLINE (formal-systems register)

*Kodo-authored (Phase A of `bm-books-rewrite`). This is the **formal systems + information/complexity + numerical
analysis** gauge — grammars & derivations, axiom footprints, the Shannon channel, Dedekind bisection $O(\log n)$,
machine $\varepsilon$. **NO physics words** (this is the null-basis partner of Vol 2 = pure geometry; their agreement
is the anti-crank evidence, so the vocabularies must stay disjoint — no force/field/orbit/mass/charge/curvature/
energy). Uses tange/funge as defined verbs (gauge 3). Walks the shared 15-ch spine (`story_arc.md`); **climaxes
Ch 11 + 14** (elaboration cost is the meter; self-application closes the loop). Every beat leans toward the **jar**
(Ch 15).*

**⚑ STATUS: SKELETON ONLY.** Chapter/section map staged so fan-out is instant once the Vol 1 outline proves the
Phase-A cadence. **Subsections are NOT yet fleshed** — each authored at the doubled depth, ONE AT A TIME, Podo
code-gates it. Do not treat any line below as gated. Register-key vocabulary (from `books/gauges/vol3_the_bound.md`):
a system whose `#print axioms` returns `[]`; proof-by-counting; **termination is a theorem** (three labels, no
fourth); dimensionless = a conserved invariant of the computation; $\alpha$ = the **self-energy / calibrated residual of a
self-reference** (self-hosting compiler, MARKED); magnitude = **uncomputable-by-counting** (the count-invariant
deformation); **continued-fraction / mediant descent** (resonance-immune root-finder, "above or below," the Euclidean step — **no halving**), **halts** at the
resolution floor (**the count-to-three floor** = 3 partial quotients of the surd `√(18/5)`; `AlphaBoundMediant` / `count3_bracket_ordered` by `decide`, the open bracket `[129.6, 137.7]` — the dyadic `AlphaBoundCountToThree`/`bound=60`/grid-128 SUPERSEDED, the mediant needs no seed); determinism + agreement between **non-communicating processes**.

---

## Chapter skeleton (the spine in Vol 3's formal-systems face)

### PART I — A DIFFERENCE
- **Ch 1 · A Difference Appears** — 1.1 the first fact (a decidable distinction; the checker resolves a symbol from a non-symbol, one bit; a *tange* of one token against another) · 1.2 the symbol as a label (the distinction IS the derivation-label; nothing allocated; a symbol means by contrast with the symbols it is not — Shannon) · 1.3 what a difference is not (not yet a count, not a number; a single case-split).
- **Ch 2 · Difference Becomes Countable** — 2.1 barely more than telling apart (the count as the cardinality of a *funge* — the bag of like tokens; enumeration) · 2.2 a number is a finite condition (a real as a nested-interval derivation, approached to a precision, never completed; machine $\varepsilon$ foreshadowed) · 2.3 the first loop (the derivation round-trips; the symbol decodes to itself — a lossless channel).
- **Ch 3 · The Residue Refuses To Disappear** — 3.1 the antagonist & treasure (the un-eliminable term; the carry the reduction cannot discharge) · 3.2 the lie of discarding (drop the residue → an unsound rule; the checker rejects) · 3.3 what the residue becomes (later read as label, count, cost).

### PART II — THE TURN
- **Ch 4 · The Tower Turns Around** — 4.1 the backward descent (elimination after introduction; unwinding the derivation) · 4.2 the seam (stage $n$ feeds stage $n+1$; the residue lives at the composition; you are billed the reductions) · 4.3 what came back changed (the normal form differs by the residue).
- **Ch 5 · The Slip Becomes A Number** — 5.1 where the loop fails to close (non-confluence; the failure-to-normalize IS the signal) · 5.2 bracketed, not guessed (bisection brackets above and below; never a fitted value) · 5.3 the reading (the invariant becomes a measured quantity).
- **Ch 6 · The Bracketed Number** — 6.1 the bound is the object (a closed rational interval; the machine owns the interval, not the point) · 6.2 the floor (machine $\varepsilon$; the resolution below which no candidate is distinguishable; the cut) · 6.3 repeatability is the honesty (deterministic rerun → same bits).

### PART III — THE FACES
- **Ch 7 · The Number Splits Into Faces** — 7.1 one derivation, four decodings · 7.2 the partition point (where the gauges cut) · 7.3 same residue, different decoding.
- **Ch 8 · The Electron Is Named** — 8.1 naming makes it measurable (the pigeonhole; finiteness forces the datum into one box) · 8.2 the stable reading (label $-1$) · 8.3 named by counting, not fiat (three labels, termination-as-theorem; the enumeration closes into the loop).
- **Ch 9 · The Corridor Rotates** — 9.1 relabeling turns number into phase (the cyclic group on three labels; the $-i$ generator as a permutation) · 9.2 label/count/order/parity/orientation (what a decoding *is* depends on the frame) · 9.3 relabeling has a cost (the permutation is billed).

### PART IV — THE CLOSING
- **Ch 10 · The Native Apparatus Presents The Residue** — 10.1 forcing the residue to show · 10.2 the physical bench (named, handed to Vol 2) · 10.3 **the computational bench (Vol 3's own cut)** — the same residue under **elaboration**, not lab hardware: the checker re-derives the datum and the reduction cost surfaces the residue.
- **Ch 11 · The Compiler Becomes The Meter** *(CLIMAX i)* — 11.1 elaboration cost is a reading (the machine measures the cost of making the reading possible; the heartbeat) · 11.2 self-reference (the instrument weighs its own act; `withHeartbeats`@SelfApplication:26 + `HeartbeatProcess`@Ep11:209 + `EKGBounded`; live survivors, NOT the atticked `AlphaProbe`/`rank_heartbeat_connection`) · 11.3 the pulse in its own currency (the heartbeat is the price of the needle).
- **Ch 12 · The Field Closes** — 12.1 the second variation (the four-channel derivation; $\delta^2$ rendered combinatorially; `Episode39ElectromagneticCoupling`) · 12.2 the dual side present (the finite three-rung structure, **MARKED to the finite, not a limit**) · 12.3 asking, not delivering (closure poses the question; hands over no magnitude). *(Vol 3's non-climax closing — kept light; the field is Vol 2's climax.)*

### PART V — THE BOUND
- **Ch 13 · Alpha As A Bracket** — 13.1 the coupling is asked (the price of one self-application; the self-energy = the calibrated residual of the machine weighing its own account, `SelfApplication` — MARKED; `RichardsonSelfNaming` ATTICKED → self-naming carried as CONCEPT only, not cited) · 13.2 the cut halted at the count (the machine does **not halve** — it cuts by the **mediant** `(p+r)/(q+s)` on its own integers, **no grid seed**; the crossing is the exact quadratic surd `d*=√(18/5)=√90/5`, so by Lagrange the continued fraction is eventually periodic `[1;1,8,1,2̄]`, convergents `1/1,2/1,17/9,19/10,55/29,…`, each a whole Euclidean step; `AlphaBoundMediant`, `count3_bracket_ordered` by `decide`, `[propext]`. **Count-to-three = 3 partial quotients `[1;1,8]` → `17/9` bracketed vs `2/1`** → the **open bracket `[129.6, 137.7]`** the strain relaxes into; count-4 → `[136.8, 137.7]`, deeper convergents settle on the machine's own `≈137.011`. Dyadic `AlphaBoundCountToThree`/`AlphaDedekind`/`bound=60`/grid-128 SUPERSEDED — the mediant needs no seed) · 13.3 the bracket, not the real ($137.036$ nameable ONLY as the external lookup value the machine **proves uncomputable-by-counting**; the machine converges on its own `≈137.011 ≠` the lab number). **⚑ SEPARATE THE TWO THREES (the correction Vol4 Ch13 makes explicit):** the bracket's three is the **CF resolution-depth** (3 partial quotients, printed and decided); the **3-point Gauss–Legendre rule is a MARKED side-diagnostic** on the second variation's finer structure (nodes $c \pm h\sqrt{3/5}$, weights $5/9\cdot 8/9\cdot 5/9$ — surds only rationally approximated), **NOT the source of the bracket and NOT "bracket = the quadrature remainder."** Conflating the two borrows certainty from a diagnostic the machine only approximates. *(Grade the `#eval` off `lake build Measurement.AlphaBoundMediant`.)*
- **Ch 14 · The Machine Measures Its Own Measurement** *(CLIMAX ii)* — 14.1 self-application (the self-hosting compiler checks its own truth) · 14.2 the witness bears the cost (the fixpoint's overhead, measured in the currency the machine runs on) · 14.3 two descriptions, one object ($1=0.999\dots$; the object with two names equal by the checker — `#print axioms = [propext]` where it appears).
- **Ch 15 · What The Machine Can And Cannot Claim** *(JAR)* — 15.1 the structure it derives (empty footprint; the multiplicity / the how-many; **nothing borrowed**) · 15.2 the magnitude it cannot see (the count-invariant deformation; uncomputable by any counter) · 15.3 the jar (the decision procedure returns exactly which digits are computable-by-counting and which are not — near side proved `[]`-footprint, far side proved uncomputable, the cut written bit by bit until it halts; agreement across non-communicating readers = the correctness argument).

---

## Discipline (Vol 3, binds every subsection at flesh-time)
- **Formal-systems first.** Grammars/derivations, axiom footprints (`#print axioms`), the Shannon channel, bisection $O(\log n)$, machine $\varepsilon$ — the actual formal objects, not prose about them.
- **NO physics words** (the null-basis constraint). No force/field/orbit/mass/charge/curvature/energy/precession. The residue is a *term/carry*, not a force; the invariant is *conserved-of-the-computation*, not a physical conservation. Vol 3 clean of Vol 2's register.
- **Tange/funge (gauge 3 uses the words):** tange = select by characteristic (a token/case picked apart by a decidable predicate); funge = bag by like (the count as cardinality of a bag of like tokens). Only where the act is genuinely one or the other.
- **Climax discipline:** Ch 11 (the meter) + Ch 14 (self-application) are the peaks. No chapter promises the magnitude; every beat leans to the jar (Ch 15).
- **Blind:** $137.036$ external-only (a lookup value), proved uncomputable-by-counting — never machine-produced. The machine's own descent converges to its OWN value `≈137.011` (the crossing `√(18/5)`), a **different** number from the lab's — no capture, no near-hit staged.
- **MARK interpretive:** the self-hosting / self-energy (`SelfApplication`) / self-reference reading = the story of *why* the structure is that structure, never worn as proved (`RichardsonSelfNaming` atticked → concept only, not cited). DERIVED core = empty footprint (`[]`), termination-as-theorem (three labels), $1=0.999\dots$, the count-to-3 bracket via `AlphaBoundMediant` / `count3_bracket_ordered` `by decide` (the mediant/CF `[129.6, 137.7]`; dyadic `AlphaBoundCountToThree`/`bound=60`/grid-128 SUPERSEDED), with the 3-point Gauss rule kept only as a **marked side-diagnostic**, NOT the bracket source (all off `#print`/`#eval` at the target build).
- **Anti-sprawl:** 15 chapters. Depth per subsection ≠ more chapters.

*(Fleshed subsections + code-gating begin at fan-out, after Vol 1 proves the Phase-A cadence.)*
