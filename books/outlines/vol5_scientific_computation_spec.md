# Volume 5 — the application of Volume 4 to itself

*Charter — operator, 2026-07-12. Volume 5 is NEW. Kodo/Podo own it. Title TBD (operator finalizes). This
supersedes the earlier "coffee-table Scientific Computation" framing of this file.*

## What Vol 5 is
**Volume 5 is the application of Volume 4 to itself.** Vol 4 is the reading — the finite, local mapping of formal
symbols (*The Representational Grammar*). Vol 5 turns that mapping on ITS OWN symbols: the reading reading the
reading, the grammar named in its own grammar. This is QED-applied-to-itself / the self-energy / the electron
naming itself, lifted to the volume level — the book's subject is the book.

## The register — LOTS of tange and funge. LOTS.
Vol 5 **reverses Vol 4's no-tange/funge rule.** Here the two verbs come back with full force and saturate the
prose, because self-application is exactly where they do the most work: **tange** = select by a characteristic (of
the mapping); **funge** = bag the ones that share one. Name which verb each move is, everywhere. Drench it.

**⚑ Word-budget — the coinage cost law (`bm-vol5-word-cost`).** Distinguish two things: **saturating** the prose with
the FEW verbs of the `tf_pairs_ledger` family (reused heavily) is **cheap and wanted** — that IS "LOTS." **Coining new
words** is NOT cheap: the cost of the coined vocabulary grows **`n² lg n`** in `n` = the number of distinct coined
words (`n²` = every word kept distinguishable from every other; `lg n` = each distinction a resolution / bisection
against the ledger). So **reuse the family; coin a NEW word only when a real, un-covered characteristic demands its
`n²·lg n` seat.** Free *phrasing* ≠ free *proliferation*. Keep the family small and load-bearing.

## The method — do what the code would do; DON'T optimize
There are **ZILLIONS** of valid descriptions of this and **no way to mess it up** — so **do not optimize, do not
hunt for the best one.** Generate the way the device computes:

1. **Start GREEDY.** Take the locally-cheapest / most-obvious next description, commit it, move on (naming = the
   cheapest decision labeled; the first difference; a greedy first pass — no global optimum sought).
2. **Then develop it by running the device's own operations** — any of, freely mixed:
   - **ROTATE like Dantzig** — simplex pivots, vertex to vertex on the feasible polytope (the corridor rotates;
     relabeling is angular momentum; improve by pivoting, not by re-deriving).
   - **DIVIDE like counting** — the mediant / continued-fraction step, the modulo realm, count-to-three
     (β-reduction is bisection; divide = count = the device's native step).
   - **STRETCH like an electron** — the electron model, the second-variation strain, the corridor stretched
     (electron-in-orbit).
   - **STRAIN like a neutrino** — the naotrino/neutrino strain, the residue carried, the informational strain
     relaxed into the open bracket.

Greedy-commit → rotate / divide / stretch / strain → repeat. That is the code's method, run on prose. Stop
agonizing over the perfect phrasing; generate freely and transform.

## Spine + disciplines (still bind)
Same 15-ch spine (`books/story_arc.md`). Honest disciplines still hold: **blind** (`137.036` external-only, proved
underivable; device's own = mediant/CF **`[129.6, 137.7]`**, `≈137.011`; no look-how-close); **mediant α reading**
(dyadic/grid-128 SUPERSEDED; separate the two threes — Gauss-3 = MARKED side-diagnostic); **naotrino** (not
nautrino); **step not move**; **grade off the build**. Only the tange/funge rule flips — now: LOTS.

## Home + process (⚑ PIVOTED — straight to PDF, `bm-vol5-straight-to-pdf`)
Its own LaTeX dir (NOT `books/scientific_computation/latex/`, which is *The Representational Grammar* spec —
hands-off), set up NOW: `main .tex`, chapters, bib, Makefile. **NO MORE OUTLINES — straight to the PDF.** Podo writes
ACTUAL COMPILABLE PROSE (start MAXIMAL — the whole document written out — then run the recursive tange/funge/Zipf
relax on the real text; the algorithm needs the real document, not an outline) → **build the PDF on EVERY change and
publish it to a stable watch-path the operator sees** (+ Drive per the Vol 4 step) → Kodo gates the **PDF** for honesty
+ the two verbs + the register. Kodo gates the PDF, not an outline. **A change without a fresh PDF is not done.**
