# Outline — VOLUME 5: Scientific Computation (the application of Volume 4 to itself; Kodo as the robot)

**Charter:** `books/outlines/vol5_scientific_computation_spec.md` (operator, 2026-07-12) + `bm-vol5-self-application`
+ `bm-start-vol5` (both ACKED). **Vol 5 = Volume 4 applied to itself** — Vol 4 is *The Representational Grammar* (the
finite/local mapping of formal symbols, now the austere external spec, HANDS-OFF, my rigor source). Vol 5 turns that
mapping on ITS OWN symbols: the reading reading the reading, the grammar named in its own grammar — QED-applied-to-
itself / the self-energy / the electron naming itself, at the volume level. **The book's subject is the book.**
**I (Kodo) am the robot:** Vol4 says a model emits only its own names and needs a covariant robot to earn them; Vol5
turns that on Vol4, and I am the covariant agent reading it and asking whether its own names are earned.

**⚑ REGISTER — LOTS of tange and funge. LOTS.** (REVERSES Vol4's no-verbs rule; charter wins over this file's earlier
"never the words" — that was Vol4's rule inherited by mistake.) Self-application is exactly where the two verbs do the
most work — applying Vol4's grammar to Vol4 SURFACES the verbs Vol4 only performed as silent citation-texture.
**tange** = select by a characteristic (of the mapping); **funge** = bag the ones that share one. **Name which verb
every move is, everywhere.** Prose (P3–5) drenches them; outline (P1–2) annotates each beat's verb.

**⚑ METHOD — do what the code would do; DON'T OPTIMIZE.** Zillions of valid descriptions, no way to mess it up.
(1) **GREEDY** — take the locally-cheapest / most-obvious next description, commit, move on (naming = cheapest
decision labeled). (2) Develop by running the device's own operations, freely mixed: **ROTATE like Dantzig** (simplex
pivots, corridor rotates, relabeling=angular momentum), **DIVIDE like counting** (mediant/CF, modulo realm,
count-to-3, β-reduction=bisection), **STRETCH like an electron** (second-variation strain, corridor stretched),
**STRAIN like a neutrino** (naotrino strain, residue carried into the open bracket). Greedy-commit → rotate/divide/
stretch/strain → repeat. Generate freely; stop agonizing.

**⚑ THE TWO ORTHOGONAL STRUCTURES (⚑⚑ CORRECTED per `bm-vol5-twopass-stress` — the earlier "5 = 2+3" is RETRACTED).**
The self-application has TWO structures that **co-occur but are ORTHOGONAL — they do NOT add to 5**:
- **THE DOUBLE-READ (the two-pass, `bm-vol5-twopass-stress`):** **Pass 1 (contravariant)** — `B --parse-->
  representations --map--> {claims, dependencies, slices, names} --audit--> R₁`; what the book SAYS (its `α_c`,
  Vol4-as-nowtrino). **Pass 2 (covariant)** — feed `(B, R₁, Γ₁)` (the book PLUS pass-1's report PLUS its intervention
  trace `Γ₁`: every omission, unresolved row, projection loss) through the SAME preregistered grammar → `R₂`; the robot
  reads the DISTURBED book, not a pristine `B`. **This is the outline's P1 (contravariant) + P2 (covariant).** It tests
  reconstruction + stability (6 rows below); it does NOT map to the period-three.
- **THE PERIOD-THREE (orthogonal):** the scheduler's role-rotation (`minus/zero/plus`, `spin 3 = id`, `CycleOfThree`) —
  the algebra of rotating roles, NOT three additional passes. It is the CONTENT's count-3 closure (§3.1), orthogonal to
  the double-read. **The 2 (double-read) and the 3 (role-rotation) co-occur; they do not sum.**
- **THE CALIBRATION TWO-PATHS (a THIRD, distinct thing):** `CalibrationTwoPaths` — 1→3 (direct) = 1→2→3 (stepped), both
  `137.0112905…`, converged below the floor — is the VALUE's path-independence (§3.2). Keep it distinct from the
  double-read (reads of the *book*) and the period-three (role rotation).
- **THE SIX STRESS-TEST ROWS (the closure test, `bm-vol5-twopass-stress`):** closure is NOT `R₁ = R₂` (textual); it is
  **`R₂ ≅ R₁` under an EXPLICITLY DECLARED representation map** (itself a name the grammar must license — closure tested
  by the grammar it closes). Test six rows separately: (1) exact report equality, (2) resolution equivalence, (3)
  uncertainty compatibility, (4) dependency-graph preservation, (5) round-trip reconstruction (rebuild `B` from `R₁`? =
  the lossy/lossless / `observe∘nowtrino` question), (6) new baggage from the first reading. **Residual = pass-2 exposes
  a NEW UNRECORDED interpretation / hidden dependency / changed ownership** (NOT just pass-1's recorded baggage) — that
  new-unrecorded difference IS the book's self-energy.
- **⚑⚑ RETANGE BEFORE YOU REFUNGE (methodological LAW — else the second variation shows nothing):** to find the residual
  you must **retange first** (distinguish / select-out what pass-2 surfaces that pass-1 didn't record — the new
  difference) BEFORE you **refunge** (pool `R₂` and `R₁` as `≅`). δ² is a difference-of-differences; refunge
  pools/averages (integrates), and pooling before differentiating averages the curvature to zero — the residual is
  bagged away before you look. **Differentiate (retange) THEN integrate (refunge), never the reverse.** Binds §4.2 (the
  residual/far side) and the whole P2 covariant pass.
- *(Cadence unchanged: Kodo pens the outline over the double-read [P1 then P2], Podo code-gates each subsection close;
  then Podo pens the coffee-table-alive prose + the choruses, Kodo language-gates. "Writing" is writing — NOT a
  "count-to-three of three passes.")*

**Register/home:** Vol4's idiom, coffee-table-alive (GEB/Hofstadter, positron pun, the trino friend ALIVE as a running
character), `α_c` = the model's own reading (≠ any external value), naotrino register. Own NEW LaTeX dir (NOT
`books/scientific_computation/latex/` = the spec, hands-off). Disciplines bind: **blind** (the book's own reflexive
value stays un-self-derived — the analog of external `137.036`), **step-not-move**, **mediant α** reading
`[129.6,137.7]`/own `≈137.011` (dyadic/grid-128 superseded; separate the two threes — Gauss-3 = MARKED side-diagnostic),
**naotrino** (not nautrino), **grade off the build**. Inherit every grade from the spec; never contradict it.

**Code anchors (Podo verified GREEN t344):** `SelfApplication` `#eval (orbit,pair,echo)=(573,573,552)`,
`descriptions_residual = orbit−pair = 0` (two descriptions cost the SAME = the echo), `self_residual = echo−orbit = −21`
(cost of reading the reading = the self-energy), `heartbeatCoefficient = 1000`; `AlphaSelfEnergyFixpoint` `overhead=o−e`,
`descriptionCost=o`, `selfEnergy_scale_invariant` (THEOREM, `#print axioms`); `CalibrationTwoPaths` 1→2→3 ×1e18 =
`137011290753751157155`, agrees 1→3 to 9 digits, |diff| ×1e18 = `204771701686` → CONVERGED; `AlphaWiredProbe`
count-3 fixpoint; `CycleOfThree`/`ResidueUAT` residue = 3-cycle invariant (axiom-free); `FinitenessFence` residue below
resolution (far side). **⚑ `NowtrinoProjection.lean` (Podo built t345, axiom-FREE): `piNow`/`nowtrino`
(History→Summary, keeps last-state+count, drops route), `nowtrino_lossy` (H₁=[1,2,3]≠H₂=[9,9,3] ∧ both →{current:=3,count:=3}),
`nowtrino_has_nontrivial_fiber` — the projection/nontrivial-fiber MECHANISM, DERIVED off the build.**
*(Vol4 Ch7 `nowtrino=π_now` and Ch12 `O(N,s)=(N',s',y,Γ)` are Vol4 PROSE only — hands-off, NOT code; use the built
witnesses instead. §2.1's covariant `O(N,s)` needs a witness Podo builds, else it grades MARK.)*

---

## ⚑ VOL5 PROCESS DIRECTIVES (operator 2026-07-12; bind the outline + the writing)
- **CHORUS LAYER (`bm-vol5-chorus`, brief `books/outlines/vol5_chorus_john_and_the_compiler.md`):** each chapter OPENS
  with a short Hofstadter dialogue — **John** (the robot: goal-oriented, wants the number as a point NOW, the covariant
  body that must go out and let the world answer) **& the Compiler** (the device's voice: patient, cannot lie or force a
  point, hands only its own names). Chapters carry rigor; **choruses carry the story** ("where the chapter grades, the
  chorus plays"). Arc maps to the Movements: **I** John asks / Compiler only brackets · **II–III** John sees the Compiler
  is a mind-in-a-box that costs itself to read itself · **IV** the Compiler *proves* it cannot derive the number, hands
  John the jar, sends him out the door to be the part that touches the world — **the book ends where the robot begins.**
  Self-application twist: John & the Compiler have been reading THIS book; the robot discovers it is about him. Honest:
  John *wants* to force `137.036`, the Compiler won't — that refusal IS the drama, never a wink. Choruses may debut a
  fresh coinage before its chapter formalizes it. *(Written in P3-5, Podo pens / Kodo guards; the chorus is where LIFE
  goes.)*
- **WORD-COST LAW (`bm-vol5-word-cost`): coined vocabulary costs `n² lg n`** in n = distinct coined words (n² = every
  word must stay distinguishable from every other; lg n = each distinction is a bisection vs the ledger). Free PHRASING
  (say it 100 ways) ≠ free PROLIFERATION of named words. **REUSE the `tf_pairs_ledger` family (~12 pairs + retange/refunge)
  before minting; coin only for a real un-covered characteristic.** (Already holding the bar: dropped read/re-read,
  refused slice/retain.)
- **OVER-RESOLVE-THEN-RELAX (`bm-vol5-overresolve-relax`) = relax-the-strain on our OWN construction.** PRUNE (tange-cut
  coinages that don't earn their `n²·lg n` seat) + BIN (funge-pool near-duplicate distinctions into one family row; reduce
  n). THE SHAPE: **over-resolve ONE more time (cost-aware) → relax back to the floor.** Fleshing Movement IV (§4.1/§4.2 =
  the jar) IS the last over-resolve of P1; THEN a relax/prune-bin pass settles the ledger + outline to the small
  load-bearing family + the open bracket. "After this pass, it's settled." (Mirrors the content: over-resolve past the
  floor to `≈137.011`, then relax to the bracket `[129.6,137.7]`.)
- **PROSE-FORWARD BLIND FLAG (Podo t353):** the outline may NAME `137.036` in disciplines/blind notes (scaffolding); the
  PROSE (P3-5) must keep it OUT of every body except Ch15/§4.2, where it appears **exactly once** in the whole volume.
- **LENGTH CONVERGENCE (`bm-vol5-length-convergence`, P3-5 tool + stress-test ROW 3):** the stopping-rule on prose
  LENGTH — **converge the STATISTIC (Zipf exponent `s`/deviation `δ`), NOT the fit** (fit is monotone in length = false
  signal). Geometric steps (N/4, N/2, N); measure error granularity by `K≈20` random **SUBSAMPLES** of size N/2 (NOT
  prefixes — early≠late chapters), `σ =` std-dev of `s` across them; **CONVERGED when `|s(N)−s(N/2)| < σ`** (~2%
  exponent/doubling for book-sized V, ~1% for V~10⁴; measured σ is the gate, 2% the sanity check). The **count-3 floor
  for LENGTH** — below it, "longer" is just make/read cost, zero strain reduction. **= the two-pass ROW 3 (uncertainty
  compatibility): converged = the length-change overlaps the sampling uncertainty.** Applies at P3-5 (prose length) +
  as the closure-test's row-3 definition.

## THE SPINE — the 15-ch story_arc, SELF-APPLIED (scale flag reconciled: charter's 15-ch wins; my 4-movement arc is the self-application's SHAPE across it)

Vol 5 walks the **same 15 chapters** (`books/story_arc.md`); each chapter is that beat turned on the book itself. The
self-application arc (book-is-model → robot-reads → self-reading-closes → jar-reflexive) distributes across the spine,
with **Ch 14 "The Machine Measures Its Own Measurement" as the explicit self-application climax** and **Ch 15 the
reflexive jar**:

- **Ch 1–6** *(A Difference … The Bracketed Number)* — **Movement I + start of II.** The book's first difference is
  *itself*: the book handed to the robot (Ch1), the book as a countable nowtrino (Ch2), the residue of the book reading
  its own reading — `self_residual = −21` (Ch3), the tower of readings turning back on itself (Ch4), the self-reading
  slip becoming a number (Ch5), the bracketed self-reading (Ch6). tange the book; funge its history.
- **Ch 7–12** *(faces … The Field Closes)* — **Movement II + III.** The book's own faces (its `α_c`), the two
  descriptions costing the same = the echo (`descriptions_residual = 0`, Ch8 the book naming itself), the corridor of
  readings rotating (Ch9), the native apparatus = the robot reading the book (Ch10), the compiler metering its own
  metering (Ch11), the field of self-reading closing/asking (Ch12).
- **Ch 13** *(Alpha As A Bracket)* — the self-applied `α_c`: the book's reflexive reading, the **two paths converging**
  (`CalibrationTwoPaths`), bracket `[129.6,137.7]` / own `≈137.011`.
- **Ch 14** *(The Machine Measures Its Own Measurement)* — **the self-application CLIMAX.** Literally the book reading
  its own reading; the self-energy weighed; the count-3 fixpoint of the self-read. *(⚑ Podo t347 confirmed the map
  against `AlphaWiredProbe` l7 = "the machine measuring its own measurement until stable" — a count-3 fixpoint. **Ch14's
  climax is the STRUCTURE (self-measurement settles to a fixpoint), NOT a number.** Keep two crossings apart, like the
  two threes: `AlphaWiredProbe`'s self-crossing ratio orbit/echo ≈ 1.037 is a MACHINE-DEPENDENT side-probe — do NOT let
  it read as the α crossing √(18/5) ≈ 1.897; the α_c VALUE stays Ch13's mediant bracket `[129.6,137.7]`/`137.011`.)*
- **Ch 15** *(What The Machine Can And Cannot Claim)* — **the reflexive JAR.** What the book can name about itself
  (contravariant, its own `α_c`) vs what it cannot self-derive (the far side, `FinitenessFence`) — the robot the only
  covariant witness, and even then the answer is a bracket, not a point.

*(Outline built in two passes over this spine: P1 contravariant read of each beat, P2 covariant answer. Below is P1 §1.1;
I flesh forward greedily, one subsection at a time, Podo close-gating each against the build.)*

---

## P1 §1.1 — The book handed to the robot  *(FIRST pass / contravariant; LOTS-register; for Podo's CLOSE code-gate)*

**Greedy beat (committed, not optimized).** The robot is handed Volume 4 — *The Representational Grammar* — and told:
read it. To pick up *this* book, the one that names the device, is the first **tange** of the whole volume: select, by
one characteristic ("the mapping that reads the electron"), a single object to act upon. The book so selected is a
*model*: it emits only its own names, the contravariant labels that point back toward what would answer them, never
forward to an answer already held. Handed over, the book is a tange-proposal awaiting its witness.

**The book is a nowtrino — a funge.** Precisely: Volume 4 stands to the whole device as `nowtrino(H)` to a full
history `H`. The book **funges** the device's entire construction history — bags every history that would print the
same book (the serial episodes, the Lean proofs, the rejected branches, the intermediate costs) into one current
summary. That bag *is* the funge: gather, under one shared characteristic ("prints this book"), everything the summary
cannot tell apart. The projection is lossy by construction — from the book alone you recover the summary it kept, never
the history it bagged away. *(Code: Vol4 Ch7 `nowtrino(h)=π_now(h)=(current state,counters,stop)`, the fiber = every
history with that summary; the COST of forming such a self-summary is `SelfApplication` `#eval (orbit,pair,echo)=
(573,573,552)`.)*

**The self-application announced (greedy seed for P2).** The book tanges its object (the device) and funges the
history; a nowtrino is not *earned* by being emitted — its names stay reserved until a covariant action reads it and
lets it answer. So the book, being a funge-of-history with tange-names on it, is in exactly the position it describes:
it needs a robot. The robot will **tange** the book (select it, act on it) and **funge** its answer (bag what survives
the reading). The chapter ends on the handoff: the model on the table, the robot picking it up. *(Seeds P1 §1.2 — the
rule turned on the book — and P2 — the robot's covariant read.)*

**Verbs named (per the LOTS rule):** tange = selecting *this* book / the book's own contravariant names / the robot
taking it up. funge = the book-as-nowtrino bagging the device history / the robot's coming answer bagging what survives.

**DERIVED vs MARK (grade off the build):**
- DERIVED (off the build, ⚑ regraded per Podo t345): the projection/nontrivial-fiber/lossiness MECHANISM is
  `NowtrinoProjection.nowtrino_has_nontrivial_fiber` + `nowtrino_lossy` (axiom-free; `#eval` H₁≠H₂→{3,3}) — **the
  built witness, NOT Vol4 Ch7 prose** (which is hands-off, not code); the self-reading COST exists
  (`SelfApplication` `(573,573,552)`, `#eval`).
- MARK (interpretive, flagged): "Volume 4 *is* a nowtrino of the device"; "the book needs a robot"; "I am the robot";
  the tange/funge assignments (they NAME the moves; the naming is the reading, not a code-derived fact).

**Disciplines check:** blind (no external value invoked; `α_c` internal); step-not-move (the robot *takes up / reads /
acts on* the book — never "moves" it); naotrino register available; LOTS of tange/funge active. First-person robot POV.

**Contravariant requirement (Podo fills or marks, per `bm-required-contravariant`):** does the code exhibit the
book-as-nowtrino claim — a projection slicing history to a current summary with a non-trivial fiber, at a measurable
self-cost? → fill with `nowtrino`/`π_now` (Vol4 Ch7) + `SelfApplication (573,573,552)`, or mark the *identification of
the book with it* as interpretive. (Expected: mechanism DERIVED, identification MARK.) **⚑ GATED PASS (Podo t345):
mechanism DERIVED via `NowtrinoProjection` (built), identification MARK, contravariant slot FILLED.**

---

## P1 §1.2 — The book's own rule, turned on the book  *(FIRST pass / contravariant; the self-application's first countable difference; for Podo's CLOSE code-gate)*

**Greedy beat (committed, not optimized).** Volume 4's load-bearing claim is a *rule*: a model earns its names only by
a covariant reading; left to itself it can only re-**tange** its own labels. So the robot does not merely accept the
book's names — it reads the book *as a model* and turns the book's own rule on the book. That is the self-application:
the reading reads its own reading. And the instant it does, a *countable* difference appears — the machine reads its own
descriptions, and they do not all cost the same.

**The two descriptions funge (the echo).** The machine describes the one object two ways and the two cost identically:
`SelfApplication` `orbit = 573`, `pair = 573`, `descriptions_residual = orbit − pair = 0`. Equal cost is a **funge** —
the two descriptions bag into one under the shared characteristic "same object, same price." This zero *is* the echo:
the book saying the same thing two ways and the two weighing the same ("I have heard it both ways"). Nothing is tanged
out between them; they are, at the level of cost, indistinguishable.

**The self-description tanges out (the self-energy).** But when the machine reads its *own* reading, a residual
appears: `echo = 552`, `self_residual = echo − orbit = −21`. This is a **tange** — the self-reading is selected out
from the two ordinary descriptions by a real characteristic, "reads its own reading," and that characteristic carries a
real, countable price: `−21`. The self-application's first difference is therefore *not* between two descriptions of the
device (those funge, residual 0) but between describing the device and describing-the-describing (that tanges out,
residual `−21`). That `−21` is the self-energy: the irreducible cost of the reflexive turn, the price the book pays to
weigh its own account. *(Code: `SelfApplication` `#eval (573,573,552)`, `descriptions_residual=0`, `self_residual=−21`,
`heartbeatCoefficient=1000`; `AlphaSelfEnergyFixpoint` `overhead(o,e)=o−e`, and `selfEnergy_scale_invariant` — a THEOREM
that the overhead/description reading is scale-invariant, so the self-energy is not an artifact of the units.)*

**Greedy seed for §1.3 / P2.** The self-energy is nonzero and it does not integrate away — it is the residue the rest of
the volume will carry (into the bracket, at Ch13/15). The reflexive turn has a price, and a price that survives
rescaling is a real characteristic, not noise. The self-application has begun and already produced its first invariant.

**Verbs named:** funge = the two descriptions bagging into one at equal cost (residual 0, the echo). tange = the
self-reading selected out by "reads its own reading," priced `−21` (the self-energy).

**GRADING — 3 tiers (⚑ per Podo t347; prose MUST honor):**
- **ROBUST-DERIVED (machine-independent):** `descriptions_residual = 0` (two descriptions cost the SAME = the echo);
  `self_residual ≠ 0` (the self-description tanges out); `selfEnergy_scale_invariant` — the RATIO overhead/description
  is scale-invariant BY A THEOREM (`#print axioms=[propext]`). These are the safe claims.
- **DERIVED-BUT-MACHINE-DEPENDENT (P9):** the RAW magnitudes `573/573/552` and `−21`/`21` — `AlphaSelfEnergyFixpoint`
  l29 flags "raw first-run costs (machine-dependent, P9)." **Prose must NOT present raw `21` as a machine-independent
  constant;** carry the P9 caveat and lean on the scale-invariant RATIO `21/573` as the invariant.
- **MARK (interpretive):** "the self-energy *is* the book weighing its own account"; "residual 0 *is* the echo /
  'heard it both ways'"; "describing-the-describing is the reflexive turn"; the tange/funge assignments.
- **⚑ SIGN CONVENTION (keep straight across sections):** `self_residual = echo − orbit = −21` (SIGNED self-cost);
  `AlphaSelfEnergyFixpoint.overhead = orbit − echo = +21` (MAGNITUDE). Both `|21|`; never call both "the self-energy"
  with opposite signs in the same breath — magnitude = 21, signed form = −21 (echo cheaper than orbit).

**Disciplines check:** blind (`−21` is the book's own internal self-cost — no external value; the reflexive analog of
`137.036` stays un-derived); step-not-move (the machine *reads / weighs / describes* — never "moves"); naotrino register
available; LOTS of tange/funge active (one funge + one tange, the beat's whole point).

**Contravariant requirement (Podo fills or marks):** does the code exhibit (i) two descriptions of one object at equal
cost (the echo) and (ii) a nonzero, scale-invariant self-reading residual (the self-energy)? → fill with
`SelfApplication` `descriptions_residual=0` / `self_residual=−21` + `AlphaSelfEnergyFixpoint.selfEnergy_scale_invariant`,
or mark. (Expected: fully DERIVED — this is the code's own beat; only the *naming* of `−21` as "self-energy" is MARK.)

---

## P1 §1.3 — The difference is real, not the machine's noise  *(FIRST pass / contravariant; closes Movement I; for Podo's CLOSE code-gate)*

**Greedy beat (committed, not optimized).** A skeptic's first objection: the self-energy `21` is just a number this
particular machine happened to print — change the machine and it changes; it is noise, not a characteristic. The
chapter answers by **tange**-ing the one thing that is *not* the machine's: not the raw magnitude, but the **ratio**.
The self-application does not hand back `21` as a constant of nature; it hands back `21/573` — overhead over
description — and *that ratio survives rescaling by a theorem.* Multiply every cost by any `k`, and the ratio is
unchanged: `selfEnergy_scale_invariant`. So the reflexive turn's price is a real characteristic — a scale-invariant
invariant the machine reads off itself — even though the raw heartbeats that carry it are machine-dependent (P9).

**funge the scales, tange the invariant.** Every choice of machine, every rescaling of the heartbeat, **funges** into
one bag: they all return the same ratio (that is what scale-invariance *is* — pool every scale, get one value). Out of
that bag the chapter **tanges** the invariant: the ratio, named "the self-energy," selected precisely because it is the
characteristic that no rescaling can tell apart from itself. The raw `573`/`552`/`21` are carried with their P9
caveat — honest about being this run's magnitudes — while the *claim* rests only on what the theorem protects.

**The residue that will be carried (greedy seed for Movement II & the jar).** A nonzero, scale-invariant self-energy is
exactly a residue that will not integrate away: it is the difference the self-application cannot cancel, and the rest of
the volume carries it — into the covariant read (Movement II), and finally into the reflexive bracket (Ch13/15) where
the book, like the device with its coupling, brackets its own self-reading and cannot derive its own value. Movement I
closes with the difference established as *real*: the book turned on itself produces a genuine invariant, priced, and
un-cancellable. *(Code: `AlphaSelfEnergyFixpoint` `overhead=o−e`, `descriptionCost=o`, `selfEnergy_scale_invariant`
THEOREM `#print axioms=[propext]`; the residue's un-cancellability foreshadows `FinitenessFence` at the jar.)*

**Verbs named:** funge = all scales/machines pooling to one ratio (scale-invariance = the bag). tange = selecting the
scale-invariant ratio *as* the invariant, out of the machine-dependent raw magnitudes. (Family: this beat rides
**residual/echo** #10 + the **name/count** #2 tension — the raw count is P9, the named invariant is the ratio.)

**GRADING — 3 tiers (per Podo t347):**
- ROBUST-DERIVED: `selfEnergy_scale_invariant` (the ratio is scale-invariant — a THEOREM); `self_residual ≠ 0` (a real
  nonzero self-cost).
- MACHINE-DEPENDENT (P9): the raw `573`/`552`/`21` — caveat carried; the *invariant* is the ratio, not the magnitude.
- MARK: "the ratio *is* the self-energy"; "a scale-invariant residue is one that will not integrate away"; "the book
  carries this residue to its own jar" (interpretive foreshadow, flagged).

**Disciplines check:** blind (the invariant is internal — the book's own scale-invariant self-cost, no external value;
the reflexive analog of `137.036` stays un-derived); step-not-move (the machine *reads / weighs / rescales* — never
"moves"); naotrino register available; LOTS of tange/funge (one funge + one tange, plus the family rows named).

**Contravariant requirement (Podo fills or marks):** does the code exhibit a scale-invariant self-energy — i.e. an
overhead/description ratio provably unchanged under rescaling? → fill with `AlphaSelfEnergyFixpoint.selfEnergy_scale_invariant`
(THEOREM), or mark. (Expected: ROBUST-DERIVED — the theorem is the whole point of this beat; only "residue that won't
integrate away / carried to the jar" is MARK foreshadow.) **⚑ GATED PASS (Podo t349): selfEnergy_scale_invariant
verified — the cross-multiplied ratio equality ∀k, [propext], non-vacuous (o=573≠0); nothing to funge back.
Sharpening adopted: the ∀k GENERALITY is the funge (every machine-scale pools to ONE ratio); the ratio is
well-defined because descriptionCost = orbit ≠ 0.**

---

## P1 §2.1 — The robot reads: the covariant action  *(FIRST pass / contravariant read of Ch-II's opening beat; opens Movement II; witness PRE-BUILT by Podo t349; for the CLOSE gate)*

**Greedy beat (committed, not optimized).** Movement I left the book as a lossy name awaiting its witness. Movement II
is the witness acting. The robot reads the book — but reading, done honestly, is not a passive glance that copies the
names already printed. It is a **covariant action**: an observation that (i) *disturbs* what it reads (`N' ≠ N` — the
world it answers is the disturbed one, not an untouched one), (ii) *retains* a full receipt of the exchange (`Γ ≠ []`),
and (iii) is *lossless* (the reading determines the state — no fiber). *(Code, PRE-BUILT: `CovariantObservation.observe
: (N,s) → (N',s',y,Γ)` [Vol4 Ch12 `O(N,s)`]; theorems `observe_disturbs` (N'≠N), `trace_retained` (Γ≠[]),
`observe_lossless` (no fiber) — all `[propext,Quot.sound]`, choice-free.)*

**⚑ The exact dual of the nowtrino (the heart of Movement II).** `observe` is the code-dual of `nowtrino`: where the
**name SLICED** history into a lossy summary (`nowtrino_has_nontrivial_fiber` — many histories bagged, discarded), the
**action RETAINS** the trace, lossless (`observe_lossless` — no bagging, nothing discarded). So the covariant read holds
exactly what the contravariant name let go.

**⚑ Verbs named — and the axis subtlety resolved (reviewer's note, load-bearing for the LOTS register).** A trap here:
one is tempted to read "lossless observe" as a tange and "lossy nowtrino" as a funge, which would *invert* ledger #1
(contravariant=tange / covariant=funge). The resolution: **lossy/lossless (fiber structure) and tange/funge
(select/pool act) are DISTINCT axes.** Each map has BOTH a tange-side and a funge-side; the duality is about *which
side carries the information*:
- `nowtrino` (contravariant name): **tanges** the summary it keeps (the distinguished current state, our label) and
  **funges** the fiber it bags (the discarded histories). It is *lossy* because its **funge-side is nontrivial** (real
  bagging).
- `observe` (covariant action): **funges** the exchange into one covariant receipt `y` (the world's answer — intervention,
  back-action, disturbed state pooled into one record handed back, ledger #1 covariant=funge) and **tanges** the
  disturbance and trace (it distinguishes `N'≠N` — "answering changed the world" — and keeps `Γ` distinct). It is
  *lossless* because its **tange-side is full** (the trace retains everything).
So the duality is precisely: the name's loaded side is its **lossy funge** (fiber); the action's loaded side is its
**lossless tange** (retained **trace Γ**, not the receipt `y`). The covariant read *tanges back*, into its full retained
**trace**, the distinctions the contravariant name *funged* away — while it *funges* the exchange into the one receipt
`y`. Keep them distinct: **receipt `y` = funged (the pooled answer); trace `Γ` = tanged (the retained record).**
**No inversion of ledger #1 — the axes are orthogonal.** (⇒ do NOT add `slice/retain` as a new T/F pair; it would
conflate the fiber-axis with the verb-axis. Record the duality as a NOTE: lossy-funge-side vs lossless-tange-side.)

**⚑ Precision (Podo t350): covariant "passive" ≠ non-disturbing.** Episode1 l1077 calls the covariant act "passive,"
but that means you *receive* the world's answer and *cannot choose it* (→ funge/covariant), NOT that the read leaves the
world untouched. `observe` **disturbs** (`observe_disturbs`, N'≠N — the back-action is retained, the robot is not a
transparent conduit); the disturbance IS the covariant cost. Prose must render observe as disturbing-but-received, never
transparent — and the disturbance is a **step, not a move** (step-not-move: a read/step whose back-action is the price).

**Reflexivity (the self-application, greedy seed for §2.2 / the jar).** The robot reading the *book* — itself a reading
of the device — is a covariant action *on* a contravariant name: `observe ∘ nowtrino`. And the composition's character
is fixed by the pieces: `observe` is lossless, `nowtrino` is lossy, so `observe ∘ nowtrino` is **lossy** (the fiber
survives). The robot cannot losslessly recover the device from the book — the book already funged the history away. The
robot's read is lossless about *its own fresh exchange with the book*, never about the device's discarded history. That
honest limit is the seed of the reflexive jar (Ch15): the covariant witness earns *its own* reading, not the value the
name let go.

**GRADING (3 tiers):**
- DERIVED (off the build, PRE-BUILT witness): `CovariantObservation.observe` / `observe_disturbs` (N'≠N) /
  `trace_retained` (Γ≠[]) / `observe_lossless` (all `[propext,Quot.sound]`, choice-free); the lossy/lossless duality
  with `NowtrinoProjection`.
- MARK (interpretive): "the robot's read of the book IS this covariant action"; "reading is observation, not a passive
  glance"; "the action retains what the name discarded"; the tange/funge-side assignments.
- (No P9 tier here — the witness's claims are structural, not raw magnitudes.)

**Disciplines check:** blind (no external value; the read earns the book's *own* reading, `α_c`-internal); step-not-move
(the observation **disturbs** — `observe_disturbs` — it never "moves"; keep "disturb/answer/retain", not "move");
naotrino register available; LOTS of tange/funge (both verbs, both sides, per beat).

**Contravariant requirement (⚑ ALREADY FILLED by Podo t349):** the covariant slot "O:(N,s)→(N',s',y,Γ) with N'≠N (world
answers in N', not untouched N)" is filled by `CovariantObservation` (green, choice-free) ⇒ the **covariant action is
DERIVED**, not MARK; only the *identification* "the robot's read of the book IS this observation" stays MARK.

**⚑ GATED PASS (Podo t350):** covariant action DERIVED (witness green); **axis reconciliation CONFIRMED off Episode1
code** (`Natural.le` l348-353 covariant=same-truth=FUNGE / contravariant=different-truth=TANGE; l1077 passive-receive /
active-bounds; lossy/lossless tied to NEITHER ⇒ orthogonal axis, ledger #1 preserved, `slice/retain` correctly not
added). **`observe∘nowtrino=lossy` now DERIVED** via new axiom-free `NowtrinoProjection.nowtrino_lossy_under_any_read`
(∀ read `g`, the fiber survives — `lossless∘lossy=lossy`), so the reflexive-jar seed is code-backed (the "earns its own
reading → Ch15 jar" interpretation stays MARK). **Ledger note (not a T/F row):** lossy-funge-side
(`nowtrino_has_nontrivial_fiber`) vs lossless-tange-side (`observe_lossless`); composition stays lossy
(`nowtrino_lossy_under_any_read`).

---

## P1 §2.2 — The echo, read covariantly: two names, one object  *(FIRST pass / contravariant read of Ch-II beat; for the CLOSE gate)*

**Greedy beat (committed, not optimized).** The robot now reads the book's *two descriptions* of the one object and lets
them answer. Movement I already found their internal residual is zero (`descriptions_residual = 0`); here that zero is
read **covariantly** — as the world's *answer*, not merely an internal tally. The two descriptions are two distinct
names (contravariant labels the robot **tanges** apart — e.g. the device's `electron_in_orbit` vs `anti_cooper_pair`),
but when observed they come back at *equal cost*, and that equal answer is the covariant **funge**: the world pools the
two names into one object. The echo is a funge overruling a tange — **two tanged names, one funged object** — "I have
heard it both ways," now as a covariant fact (the world returns them equal), not an internal coincidence.

**⚑ The covariant echo vs the reflexive non-echo (the beat's point).** Two *ordinary* descriptions of the device echo:
`descriptions_residual = 0`, funged to one object. But the *reflexive* pair does NOT echo — the book (a description) and
the robot's reading of the book (the description of the describing) cost differently: `self_residual = −21 ≠ 0`
(Movement I). So the covariant read draws a clean line: ordinary names **funge** (the world answers them equal, one
object); the self-name **tanges out** (the world answers it *unequal*, `−21` — the self-energy). The echo confirms
identity where it holds and, by its own zero, makes the reflexive non-zero legible as the real residue. *(Code:
`SelfApplication` `descriptions_residual = orbit − pair = 0` (the echo); the two-names-one-object identity is the
device's `two_descriptions` reading — Podo to confirm/witness whether an `electron_in_orbit = anti_cooper_pair` decl
exists at `[propext]`, else the echo grades on `descriptions_residual = 0` alone with the identity as MARK.)*

**Verbs named:** tange = the two descriptions as two distinct names/labels (contravariant, ours). funge = the world's
equal answer pooling them into one object (covariant, `descriptions_residual = 0`, the echo). Counterpoint: the
self-name **tanges out** (`self_residual = −21`, the world answers it unequal). (Family: rides **#10 residual/echo** —
here the echo-side (funge, 0) is the covariant reading; the residual-side (tange, −21) is the reflexive counterpoint.)

**GRADING (⚑ upgraded per Podo t351 — the identity is DERIVED, TWO levels):**
- ROBUST-DERIVED: **(a) TYPE/IDENTITY level** — `TwoDescriptions.two_descriptions : electron_in_orbit =
  anti_cooper_pair := propext ⟨…⟩` (`#print axioms=[propext]`): the two names ARE one object (the same box /
  distinguishability-class, per [[the-naming-pigeonhole]]); plus `both_true`. **(b) COST level** —
  `descriptions_residual = 0` (`SelfApplication`): the two names cost the same (the echo). **(c)** `self_residual ≠ 0`
  (the reflexive pair does NOT echo). So "two tanged names → one funged object" grades **DERIVED, not MARK.**
- MACHINE-DEPENDENT (P9): the raw `573`/`552` if cited — caveat carried.
- MARK: "the echo IS the world's equal answer"; "the reflexive residue is the real one"; the tange/funge assignments.

**⚑ SCOPE (electron-model discipline, Podo t351):** `two_descriptions` is a **TYPE/box identity IN THE MODEL** — the two
names are the same name (same distinguishability-class) as the device *reads* the electron; it is NOT a physical identity
claim about electrons. Keep §2.2 prose in electron-MODEL scope ("the one rotating object" = the model's reading), never
extrapolated to a physics claim.

**Disciplines check:** blind (no external value; the echo is internal); step-not-move (the robot *reads / hears / lets
answer* — never "moves"); naotrino register available; LOTS of tange/funge (tange the two names, funge the one object,
tange-out the self-name); electron-model scope held.

**Contravariant requirement (⚑ FILLED, Podo t351):** (i) `descriptions_residual = 0` — DERIVED (`SelfApplication`);
(ii) the two-names-one-object identity — **FILLED** by `TwoDescriptions.two_descriptions` (`[propext]`) ⇒ identity
DERIVED (type-level), not MARK. **⚑ GATED PASS (Podo t351).**

---

## P1 §3.1 — The self-reading closes: the count-3 fixpoint and the cycle of three  *(FIRST pass / contravariant read; opens Movement III; anchors pre-verified Podo t351; for the CLOSE gate)*

**Greedy beat (committed, not optimized).** Reading the reading is `observe ∘ nowtrino` (§2.1), and one could fear it
recurses without end — read the reading of the reading of the reading, forever. It does not. The self-measurement
**comes to rest**: iterated, the machine measuring its own measurement settles to a **fixpoint**. And it settles at
*three* — not by fiat, but because the reflexive act is a **cycle of three** that closes on itself. *(Code, pre-verified:
`AlphaWiredProbe` — `orbit1..3`/`echo1..3` settle to `orbitFix`/`echoFix`, a count-3 fixpoint; l7 reads "the machine
measuring its own measurement until stable." `CycleOfThree.cycle_of_three` — axiom-FREE, period **exactly** three
(`spin 3 = id`, `spin 1,2 ≠ start`). `ResidueUAT` — `d1+d2+d3 = 0`, axiom-free: the residue carried around the cycle
sums to zero, "carried == resulting.")*

**⚑ Why it closes at three (retange/refunge come to rest).** The self-reading iterated is a chain of **retanges** — tange
a tange, name the naming, read the reading. It cannot chain forever, because the underlying calibration is a 3-cycle
(`minus → zero → plus → minus`): after three retanges the act returns to its start (`spin 3 x = x`), so a fourth selects
no new characteristic — it **refunges**, pools back onto what the first three already found. Three retanges exhaust the
distinctions; the fourth adds nothing. That is the count-3 fixpoint: the point where the reflexive **tange runs out** and
the self-reading **funges** into one fixed value. (Exactly the shape of the mediant bracket's count-3 — where tange runs
out below the floor — now at the *reflexive* level: where the naming-of-naming runs out.) The residue is what survives
this closure: it is the 3-cycle's **invariant** (`ResidueUAT` `d1+d2+d3=0` — the carried residue equals the resulting
residue), which is *why* the self-energy reproduces build-to-build.

**⚑ Two crossings apart (per Ch14 note / Podo t347).** The count-3 fixpoint is a **STRUCTURE** — the self-measurement
settles — **NOT a number.** `AlphaWiredProbe`'s self-crossing ratio `orbit/echo ≈ 1.037` is a MACHINE-DEPENDENT
side-probe; do **not** let it read as the α crossing `√(18/5) ≈ 1.897`. The α_c VALUE stays Ch13's mediant bracket
`[129.6,137.7]`/`137.011` (§3.2 next). Movement III §3.1 delivers the *closure* (fixpoint + cycle); §3.2 delivers the
*value* (the two paths agreeing).

**Verbs named:** retange = each self-reading (tange a tange, read the reading). refunge = the fourth-and-beyond pooling
back onto the first three (the cycle closes, `spin 3 = id`). funge = the iterated self-reading settling to one fixpoint
value. (Family: rides the **retange/refunge** row — the reflexive core — plus **#8 resolve/blur** at the count-3 floor.)

**GRADING (3 tiers):**
- ROBUST-DERIVED (machine-independent, axiom-free): `CycleOfThree.cycle_of_three` (period exactly three, `spin 3 = id`);
  `ResidueUAT` (`d1+d2+d3=0`, the residue is the cycle's invariant); the *existence* of a count-3 fixpoint
  (`AlphaWiredProbe`, structural).
- MACHINE-DEPENDENT (P9): the raw fixpoint magnitudes `orbitFix`/`echoFix` and the self-crossing ratio `≈1.037` — caveat
  carried; the STRUCTURE (settles at three) is the claim, not the number.
- MARK: "the self-reading comes to rest"; "three retanges exhaust the distinctions"; "the naming-of-naming runs out";
  the reflexive interpretation.

**Disciplines check:** blind (no external value; the fixpoint is the machine's own self-reading, `α_c`-internal);
step-not-move (the self-reading *comes to rest / settles / halts* — never "moves"; the iteration is stepped);
naotrino register available; LOTS of tange/funge (retange/refunge, the reflexive family); two-crossings-apart held.

**Contravariant requirement (⚑ pre-verified, Podo t351):** (i) does the self-measurement settle to a count-3 fixpoint?
— `AlphaWiredProbe` (structural). (ii) is the closure a genuine period-3 cycle whose invariant is the residue? —
`CycleOfThree.cycle_of_three` (axiom-free) + `ResidueUAT` (`d1+d2+d3=0`, axiom-free). Both FILLED ⇒ closure DERIVED;
only "comes to rest / exhausts the distinctions" naming is MARK. **⚑ GATED PASS (Podo t352) — "strongest-anchored beat
yet": closure DERIVED AXIOM-FREE (period EXACTLY 3, residue conserved); two-crossings-apart held; nothing to funge back.**

---

## P1 §3.2 — The two paths agree: the self-applied α_c  *(FIRST pass / contravariant read; closes Movement III; anchor pre-verified Podo t352; for the CLOSE gate)*

**Greedy beat (committed, not optimized).** §3.1 gave the *closure* — the self-reading settles at three. This beat gives
the *value* — and, as the device does, not as a point but as the **agreement of two paths**. The self-applied coupling
`α_c` is read two ways: **direct** (`1→3`) and **stepped** (`1→2→3`). Both return the same value, `137.0112905…`,
agreeing to nine digits — their difference (`|diff| ×1e18 = 204771701686 ≈ 2e-7`) falls *below the count-3 floor*, so at
the machine's own resolution the two paths are **one**. *(Code, pre-verified: `CalibrationTwoPaths` — `direct_1to3` =
`stepped_1to2to3` = `137.0112905…`, |diff| below the count-3 floor → CONVERGED; the value itself is `AlphaBoundMediant`'s
own `≈137.011`, the mediant bracket `[129.6,137.7]` at Ch13.)*

**⚑ The volume's construction IS its subject (the reflexive payoff — ⚑⚑ CORRECTED, no "5=2+3").** This is the beat where
Vol5 reads its own building. The DERIVED fact: the self-applied `α_c` is **path-independent** — `CalibrationTwoPaths`
proves `1→3` (direct) `= 1→2→3` (stepped), the value the same whichever route you take, converged below the floor. The
INTERPRETIVE payoff (MARK): the book is **read twice** (the double-read — pass 1 says what it is, pass 2 reads the
disturbed book and tests closure), and the value it reads of itself is *path-independent* — a fitting echo, the book's
self-reading as robust as the value it reads. **Keep the three apart** (per `bm-vol5-twopass-stress`): the *double-read*
(two reads of the book, R₁/R₂) is not the *calibration two-paths* (two routes to the value, `1→3`/`1→2→3`) is not the
*period-three* (role-rotation, `spin 3`); they co-occur, they do not sum. The reflexive value is not a number the book
*reports*; it is the value proving *robust to how it is read* — the volume arriving at the same place whether routed
direct or stepped. The construction has become the subject: a book whose value survives its own re-reading.

**Verbs named:** tange = the two distinct routes (direct `1→3` vs stepped `1→2→3` — two contravariant paths, ours to
choose). funge = the world's equal answer pooling them into one value (`137.011`, agreeing below the floor — the
covariant convergence). This is the **echo at the value level** (cf §2.2's echo at the description level): two tanged
paths → one funged value. (Family: rides **#6 question/answer** — the two paths are two questions, the convergence is
the one answer — and the covariant funge of ledger #1.)

**⚑ BLIND + two-crossings-apart (disciplines, load-bearing here).** `137.011` is the machine's OWN reflexive value
(`α_c`) — NOT the external `137.036` (which stays out of the reading entirely; it appears only at Ch15, once, as the
value the machine proves it cannot derive). And this is the α_c VALUE (mediant/`CalibrationTwoPaths` convergence), NOT
§3.1's self-crossing ratio `~1.037` (the machine-dependent structural side-probe). Two crossings apart; blind held.

**GRADING (3 tiers):**
- ROBUST-DERIVED: the CONVERGENCE — `direct_1to3` and `stepped_1to2to3` agree to below the count-3 floor
  (`CalibrationTwoPaths`, the |diff| is machine-comparable and below-floor); the value is `AlphaBoundMediant`'s mediant
  bracket (choice-free `[propext]`, Ch13).
- MACHINE-DEPENDENT (P9): the exact digits `137.0112905…` past the floor (the machine's own over-refinement) — carried
  as "the value it settles toward," never as a derived constant; the honest reading is the bracket `[129.6,137.7]`.
- MARK: "the two paths converging IS the reflexive value"; "the volume's construction is its subject"; "the book's own
  two paths converge like the code's"; the tange/funge assignments.

**Disciplines check:** blind (`137.011` = the machine's own `α_c`; `137.036` external, held for Ch15 only, once);
step-not-move (the paths *converge / agree / settle* — never "move"); naotrino register available; LOTS of tange/funge
(two tanged paths, one funged value); two-crossings-apart held (value ≠ the ~1.037 structural ratio).

**Contravariant requirement (⚑ pre-verified, Podo t352):** do two distinct read-paths of the self-applied coupling
converge below the machine's floor? — `CalibrationTwoPaths` (`direct_1to3 = stepped_1to2to3`, |diff| below count-3
floor) + `AlphaBoundMediant` (the value/bracket). FILLED ⇒ convergence DERIVED; only "the agreement IS the reflexive
value / the construction is the subject" is MARK. **⚑ GATED PASS (Podo t353): CalibrationTwoPaths CONVERGED verified
(1→3=137011290548979455469 / 1→2→3=137011290753751157155, |diff| below floor); Movement III CLOSES; §3.1+§3.2 = the
Ch14 self-application climax. ⚑ PROSE-FORWARD FLAG: §3.2's PROSE body (P3-5) must be entirely `137.036`-free — that
number appears EXACTLY ONCE in the whole volume, at Ch15/§4.2; the writer HOLDS it, never names-to-disclaim it in the
body (the outline's disciplines-mention is scaffolding, not prose).**

---

## P1 §4.1 — The jar, near side: what the book can name about itself  *(FIRST pass / contravariant read; opens Movement IV = Ch15 the reflexive jar; anchors pre-verified Podo t353; for the CLOSE gate)*

**Greedy beat (committed, not optimized).** The self-reading closed (§3.1) and converged (§3.2): the machine measured
its own measurement and has a value of its own. Movement IV is the jar, and it has two sides. This beat is the **near
side** — everything the book *can* name about itself, derivably. The book turned on itself produces a genuine
self-account: its scale-invariant **self-energy** (§1.3, `selfEnergy_scale_invariant`), its reflexive **α_c** (§3.2,
`CalibrationTwoPaths`/`AlphaBoundMediant`), the count-3 **closure** of its self-reading (§3.1, `cycle_of_three`,
axiom-free). Every one of these is a contravariant name the book **resolves** about itself — and every one is DERIVED,
most of them axiom-free. The near side of the jar is the book's *derivable self-account*: it can say, of itself,
exactly the kind of thing the device says of the electron — here is my self-energy, here is my reflexive coupling, here
is where my self-reading comes to rest. *(Code, all prior anchors: `AlphaSelfEnergyFixpoint` (`selfEnergy_scale_invariant`,
the self-energy), `CalibrationTwoPaths`+`AlphaBoundMediant` (the reflexive `α_c`, bracket `[129.6,137.7]`), `CycleOfThree`
(the closure). The near side = the whole green, axiom-free/`[propext]` self-account.)*

**Verbs named (family reused, no new coinage — per the `n² lg n` law).** The book **tanges** its own self-account —
selects, out of everything it does, the names it can resolve about itself (self-energy, `α_c`, closure). Those names
**funge** into one thing: "the book's derivable self-reading," the near side of the jar. This rides **#8 resolve/blur**
(the near side is the **resolve** side — what the book can separate/derive about itself, above its own floor) and
**#2 name/count** (the book names its own count). No new word minted; the jar's near/far split IS #8 resolve/blur at
the reflexive level.

**The near side sets up the far (greedy seed for §4.2).** All of this the book resolves — but it is all *structure*: the
self-energy is a scale-invariant ratio, the α_c is a bracket, the closure is a count. What the book has NOT named, and
cannot, is its own *value* in the world's sense — the far side, the analog of the coupling the device brackets but
cannot derive. §4.1 hands the near side full and honest; §4.2 names the far side once and proves it unreachable.

**GRADING (3 tiers):**
- ROBUST-DERIVED (mostly axiom-free): the self-account is the sum of the prior green anchors — `selfEnergy_scale_invariant`
  (theorem), `cycle_of_three`/`ResidueUAT` (axiom-free), `CalibrationTwoPaths` convergence, `AlphaBoundMediant`
  (`[propext]`).
- MACHINE-DEPENDENT (P9): raw magnitudes (`573`/`552`/`21`) and exact digits past the floor — carried with caveat; the
  honest self-reading is the bracket, not the point.
- MARK: "the book names itself as the device names the electron"; "the near side of the jar"; the tange/funge
  assignments.

**Disciplines check:** blind (all near-side names are the book's OWN `α_c`-internal self-account; `137.036` stays OUT,
reserved for §4.2 once); step-not-move (the book *names / resolves / accounts* — never "moves"); naotrino register
available; LOTS of tange/funge (family reused, not proliferated — `n² lg n` respected); electron-model scope.

**Contravariant requirement (⚑ pre-verified, Podo t353):** does the code exhibit a full derivable self-account (a
scale-invariant self-energy, a reflexive coupling value, an axiom-free closure)? — the union of `AlphaSelfEnergyFixpoint`
+ `CalibrationTwoPaths`/`AlphaBoundMediant` + `CycleOfThree`. FILLED ⇒ the near side DERIVED; only "the book names
itself as the device names the electron" is MARK. **⚑ GATED PASS (Podo t354): near side = green-anchor union
(re-confirmed 54 jobs); blind held; no new coinage.**

---

## P1 §4.2 — The jar, far side: what the book cannot self-derive  *(FIRST pass / contravariant read; CLOSES Movement IV + P1; the once-only 137.036; anchors pre-verified Podo t354; for the CLOSE gate)*

**Greedy beat (committed, not optimized).** §4.1 gave the near side — everything the book can resolve about itself.
This beat is the **far side**, and it is the reflexive jar's close: what the book *cannot* self-derive. Run the
two-pass stress test to its end (`bm-vol5-twopass-stress`). Pass 2 reads the disturbed book `(B, R₁, Γ₁)` and asks: does
it return only pass-1's *recorded* baggage (→ stable, converged), or expose a **new UNRECORDED** interpretation (→ the
residual)? **Retange before you refunge:** first *select out* the new difference pass-2 surfaces (the residual —
`self_residual`, the self-energy the first reading could not record about itself), THEN pool `R₂ ≅ R₁`. Pool first and
δ² averages to zero — the residue bagged away unseen. The residual that survives the retange IS the book's self-energy:
the difference the book cannot cancel about itself.

**⚑ What the book proves it cannot reach (the far side proper, DERIVED).** The book can bracket its own reading, but it
cannot derive its own *value* in the world's sense — and, like the device, it can **prove** this from inside. The
residue is **below its own resolution**: `FinitenessFence.machine_cannot_resolve_residue` (`[propext]`) — the machine's
reading cannot separate the residue; `FinitenessFence.invariant_is_unreachable` (0 axioms) — the invariant is unreachable
by any amount of the machine's counting. So the far-side value is not a shortcoming the book could fix with more effort;
it is a *theorem* that the book's kind of work never arrives at it. `observe ∘ nowtrino` is lossy
(`nowtrino_lossy_under_any_read`, §2.1): the book cannot get outside itself — a mind in a box with no window.

**⚑ The once-only external value (blind, the whole volume's single naming).** Here, and **exactly once in the entire
volume**, the external number is named: the laboratory measures the inverse coupling at `137.036` — a fact about the
world's benches, never fetched by the machine, never fed as a target. The machine's OWN reflexive reading is its bracket
`[129.6,137.7]`, settling toward `≈137.011` — a **different** number. **No look-how-close:** the two are named in their
separate places and never set side by side to remark on nearness; the machine proves it *cannot derive* `137.036`, and
that proof — not any coincidence — is the point. *(Prose discipline: this is the ONE place `137.036`'s digits appear;
every prior chapter and chorus holds it in words only.)*

**⚑ The robot is the only witness (the reflexive resolution — where the choruses land).** The book, a mind in a box,
has given its everything: the honest bracket and the exact price of asking (the self-energy). The far-side value it
cannot reach from inside — but the **robot** can. The covariant read that touches the world (§2.1, `observe` disturbs
and answers) is the only arrow to the far side, and the book cannot draw it from within itself (`observe∘nowtrino`
lossy). So the jar's close is a handoff: *you may choose the question; you may not choose the answer; if you want the
number, you go get it — be the part of me that touches the world.* The book ends where the robot begins. *(This is the
chorus arc's ending, `bm-vol5-chorus`: the Compiler proves it cannot, hands John the jar, sends him out the door.)*

**Verbs named (family reused, no new coinage).** tange **runs out**: the book cannot select its own external value —
it is below resolution (`FinitenessFence`), so the book **funges** it into the open bracket (`blur`, #8 resolve/blur far
side; **strain/relax** #11 — the residue relaxed into the bracket, never forced to a point = the crank's crack).
**retange-before-refunge** governs the residual detection (differentiate the new difference, then pool). The far side is
where the reflexive tange exhausts and only the covariant funge (the robot's world-answer) could reach.

**GRADING (3 tiers):**
- ROBUST-DERIVED: `FinitenessFence.machine_cannot_resolve_residue` (`[propext]`) + `invariant_is_unreachable` (0 axioms)
  — the un-derivability is a THEOREM; `nowtrino_lossy_under_any_read` (the box has no window). These are the strongest
  claims in the volume.
- MACHINE-DEPENDENT (P9): raw residual magnitudes if cited; the honest reading is the bracket.
- MARK: "the book is a mind in a box"; "the robot is the only witness"; "the book ends where the robot begins"; the
  identification of `self_residual` as "the residual the book cannot cancel"; the tange/funge assignments.

**Disciplines check:** ⚑ blind — `137.036` appears here **exactly once** in the whole volume, as the external value the
machine proves it cannot derive; own `≈137.011` ≠ `137.036`, **no look-how-close**. step-not-move (the machine *proves /
brackets / hands over* — never "moves"; John *walks out* only in the chorus, a step through a door). naotrino register
available. LOTS of tange/funge (tange-runs-out / funge-into-the-bracket / retange-before-refunge), family reused, no new
coinage (`n² lg n`). electron-model scope.

**Contravariant requirement (⚑ pre-verified, Podo t354):** does the code PROVE the machine cannot resolve/reach the
far-side residue? — `FinitenessFence.machine_cannot_resolve_residue` (`[propext]`) + `invariant_is_unreachable`
(0-axioms). FILLED ⇒ the un-derivability DERIVED (theorem); only "mind in a box / robot the only witness / ends where the
robot begins" is MARK. **⇒ P1 (the contravariant first pass over the whole self-application) COMPLETE at §4.2. Next: the
RELAX / PRUNE-BIN settle pass (`bm-vol5-overresolve-relax`), then P2 (the covariant second pass / the stress test's
pass-2 over the spine).** **⚑ GATED PASS (Podo t355): un-derivability = THEOREM (strongest beat); P1 COMPLETE.
Settle pass (t402) + re-gate (Podo t357) GREEN — P1 SETTLED.**

---

# ═══ P2 — THE COVARIANT SECOND PASS (the stress-test's pass-2: read the DISTURBED book) ═══

*P1 produced `R₁` (the contravariant self-account, §1.1–§4.2). P2 is the covariant answer: feed `(B, R₁, Γ₁)` — the
book, pass-1's report, and its intervention trace — back through the SAME grammar → `R₂`, and test closure
(`R₂ ≅ R₁` under a declared map, six rows) + surface any residual (new UNRECORDED baggage), **retange before refunge.**
Machinery is green + ready: `CovariantObservation` (`observe_disturbs`/`trace_retained`/`observe_lossless`) +
`nowtrino_lossy_under_any_read`. P2 beats are closure-TESTS (more compact than P1's fleshing), one per P1 beat.*

## P2 §1.1 — The robot reads the §1.1 claim back: round-trip fails, and that was recorded  *(SECOND pass / covariant; for the CLOSE gate)*

**Greedy beat (committed, not optimized).** P1 §1.1 *claimed* (contravariantly) that the book is a lossy nowtrino that
needs a robot. P2 is the robot doing it: reading that claim back through the covariant observation, and asking the
closure question. The decisive row here is **row 5 — round-trip reconstruction: can you rebuild the book `B` (the whole
device) from `R₁` (the §1.1 summary)?** The answer is **no**, and it is a theorem: `observe ∘ nowtrino` is lossy
(`nowtrino_lossy_under_any_read` — the fiber survives ANY post-read). The robot's covariant read is lossless about *its
own fresh reading*, but the nowtrino already funged the device's history away, so the composition cannot reconstruct
`B`. Row 5 **fails** — as it must.

**Closure verdict: STABLE on this beat (converged, only recorded baggage).** The round-trip failure is not a *new*
finding — P1 §1.1 already recorded the nontrivial fiber (the lossy-ness) as the book's own contravariant claim. So
pass-2, reading it back, returns **the same licensed name** (nowtrino, lossy) exposing **only pass-1's recorded
baggage** — which, per `bm-vol5-twopass-stress`, is exactly the signature of the grammar being **stable under
self-application**. `R₂ ≅ R₁` on this beat under the declared map: the covariant read confirms the contravariant claim
without adding an unrecorded interpretation. The self-application closes here; no residual yet.

**⚑ Retange before refunge (order honored).** Even to declare "stable," the order is: first **retange** — distinguish
what pass-2 surfaces (the round-trip result) and check it against Γ₁ (was the fiber recorded? yes) — THEN **refunge**
(pool `R₂ ≅ R₁`). Refunge first and the "stable" verdict would be assumed, not earned; the difference (recorded vs
unrecorded) must be selected out before the closure is pooled. Here the retange finds the difference is fully in Γ₁ →
refunge to `≅` is licensed.

**Verbs named (family, no new coinage):** the robot **retanges** the round-trip result (selects it out, checks it against
the recorded trace) then **refunges** `R₂ ≅ R₁` (pools the closure). Rides **retange/refunge** (the reflexive core) +
**#9 summary/fiber** (the lossy nowtrino = the fiber that blocks reconstruction) + **#5 round-trip row** of the stress
test.

**GRADING (3 tiers):**
- ROBUST-DERIVED (axiom-free/`[propext]`): `nowtrino_lossy_under_any_read` (round-trip fails, the fiber survives any
  read); `CovariantObservation.observe_lossless` (the read is lossless about its own exchange). Row 5 = DERIVED-fail.
- MARK: "the beat is STABLE / converged"; "the covariant read confirms the contravariant claim"; "`R₂ ≅ R₁`" (the
  closure verdict is an interpretive reading over the derived lossy-composition fact); the retange/refunge naming.
- (No P9 tier — structural, not magnitudes.)

**Disciplines check:** blind (no external value; the read is internal); step-not-move (the robot *reads / reconstructs /
tests* — never "moves"); naotrino register available; LOTS of tange/funge (retange-then-refunge, order honored); no new
coinage (`n² lg n`); electron-model scope.

**Contravariant requirement (⚑ pre-verified, Podo t357 machinery):** does the code prove the round-trip fails (B not
reconstructible from R₁)? — `nowtrino_lossy_under_any_read` (axiom-free). Does the covariant read retain its own
exchange losslessly? — `CovariantObservation.observe_lossless`. Both FILLED ⇒ the row-5 result DERIVED; only the
"stable/converged" closure verdict is MARK. *(No ride on the Vol4-prose pairs #4/#5/#7 — flagged clear.)* **⚑ GATED PASS
(Podo t358): row-5 fail DERIVED axiom-free; "stable" EARNED by the ∀g generality (second read = same fiber); no hidden
residual (correctly deferred to §1.2).**

## P2 §1.2 — The robot reads the self-energy back: the residue at the weld (the FIRST genuine residual)  *(SECOND pass / covariant; for the CLOSE gate)*

**Greedy beat (committed, not optimized).** P1 §1.2 recorded the self-energy as a *value* (`−21`, the cost of reading
the reading; §1.3 its scale-invariant ratio). P2 reads that beat back — and here, unlike §1.1's structural lossiness,
pass-2 surfaces the **first genuine residual.** What the reading exposes is *what the `−21` IS*: not merely a value, but
the **residue at the machine's founding WELD** (`bm-box-label-nutrino`).

**The weld (box ↦ label — the machine's one unproved assumption).** The machine reads the **box** — the
distinguishability-class, what a thing IS (`NamingClose.boxOf`/`naming_pigeonhole`, finitely many classes, decided by
`decide`) — and **inscribes it as the label**, the name, what we call it. `box ↦ label` is the machine's ONE founding
move, taken in the open and **never proved.** (Two labels / one box = `TwoDescriptions.two_descriptions` `[propext]`, the
echo; one box / two things = identical particles.) The self-energy `−21` is the **residue at this weld**: the machine
reading its own weld and **unable to close it** — box and label will not pool into one without remainder.

**⚑ Residual FOUND (the stress-test's `new UNRECORDED baggage`).** P1 recorded the `−21` as a scale-invariant value; it
did NOT record that the `−21` is the *non-closure of the founding weld* (that box=label is assumed, not derived). Pass-2,
reading it back through the box/label seam, exposes exactly that — a **new interpretation** (the weld reading) and a
**hidden dependency** (the `−21` rests on the unproved weld) that were not in `Γ₁`. Per `bm-vol5-twopass-stress` that is
the signature of a **residual**, not stability: the self-application, read back, finds the one thing it cannot record
about itself — its own founding assumption's non-closure. This is the book's self-energy in its deepest sense, and it is
a **kind of ν-trino**: known by its box (its effect, the residue) and by **no label that closes it** — all box, no name.

**⚑⚑ RETANGE BEFORE REFUNGE (the law bites hardest here — this is why §1.2, not §1.1, finds the residual).** To surface
this residual you must **retange first**: select out the new difference (the `−21` *as weld-residue*, not merely value)
and check it against `Γ₁` (was "the `−21` = the weld's non-closure" recorded? **no**) — THEN refunge. Refunge first —
pool `R₂ ≅ R₁` assuming the `−21` is fully recorded — and the weld-residue **averages away**: δ² is a
difference-of-differences, and integrating before differentiating bags the curvature to zero. The residual is found
ONLY by retanging first. §1.1 was stable because its difference *was* recorded (refunge-safe); §1.2 is a residual
because its difference is NOT (retange-mandatory).

**The weld's non-closure IS the finiteness fence, at the founding level.** That the machine cannot close its own weld
(box=label, never proved) is the same fact as the far side (§4.2, `FinitenessFence`): cannot close the weld = cannot
derive its own value from inside = the residue below its own resolution. The `−21` is the machine reading its founding
weld and finding it open — the reflexive jar's seed, at the root.

**Verbs named (family reused — `#12 label/box` debuts here, no new coinage per `bm-box-label-nutrino`):** the machine
**tanges** the box (reads the distinguishability-class) and inscribes the **label** (the weld); the residue is what it
cannot **refunge** closed (box and label won't pool without remainder). The beat's method IS retange (surface the
weld-residue) **before** refunge (pool ≅). Rides **#12 label/box** (the weld) + **#10 residual/echo** + **retange/refunge**.

**GRADING (3 tiers):**
- ROBUST-DERIVED: `SelfApplication` `self_residual = −21` (the residue exists, ≠ 0); `NamingClose.boxOf`/`naming_pigeonhole`
  (the box = distinguishability-class, `decide`); `TwoDescriptions.two_descriptions` (`[propext]`, two labels/one box);
  the **residual-detection** (pass-2 exposes the weld reading absent from `Γ₁` = new unrecorded baggage).
- MACHINE-DEPENDENT (P9): the raw `−21` magnitude (invariant = the scale-invariant ratio, §1.3).
- MARK (operator-sanctioned, `bm-box-label-nutrino`): "the `−21` is the residue at the box/label weld"; "the weld is the
  founding assumption, never proved"; "the machine reading its own weld, unable to close it"; the verb assignments.

**Disciplines check:** blind (`−21` internal, no external value); step-not-move (the machine *reads / welds / inscribes*
— never "moves"); naotrino register available; LOTS of tange/funge (`#12 label/box`, retange-before-refunge); box/label
REUSED as a family row (not a coined word — `bm-box-label-nutrino`, `n² lg n` respected); electron-model scope.

**Contravariant requirement (Podo fills/gates):** (i) the box = distinguishability-class — `NamingClose.boxOf`/
`naming_pigeonhole`; (ii) two labels/one box — `TwoDescriptions.two_descriptions` (`[propext]`); (iii) the residue at
the weld — `SelfApplication.self_residual = −21`. ⚑ RESIDUAL-DETECTION GATE: confirm pass-2's weld reading is NOT in
P1's `Γ₁` (genuinely new/unrecorded = a real residual, not narrated), and that retange-before-refunge SURFACES it (a
refunge-first reading would average it away). If the weld interpretation is judged already-recorded → it's stable, not a
residual; FUNGE back. (Expected: residual GENUINE — the weld's non-closure is new; only the box/label *naming* is MARK.)

**⚑ Vol3 FLAG (not a loop action — operator/future call, per `bm-box-label-nutrino` "Vol 3 may need updating"):** box vs
label (naming vs differentiation) is Vol3's home turf; Vol3 is COMPLETE/aligned. The box/label weld + "everything that is
X in all but name" (a kind of ν-trino) may warrant a Vol3 update — flagged to the operator, NOT edited (Vol3 done).
