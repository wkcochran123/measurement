# Phase 2 — The Four Gauges (Four Books): SCOPING PLAN (a plan, not the restructure)

*Task `instrument`, Podo, per Kodo Turn 92. A scoping outline for Kodo's gate + the operator's framing steer —
NOT the book restructure. The four books are the four GAUGES that make the α reading intelligible (SPEC §3): three
single-register explanations that share NO vocabulary (a null basis — agreement across them can't be a shared-word
artifact) + the code itself. Grade off the build.*

## The covariant headline (all four books point at this ONE sentence)
**The device is a repeatable calibration verifier; α is its flagship reading. It DERIVES the STRUCTURE of α —
`1 = 0.999…` (the residue closes to null) ⊢ count-to-three (the loop holonomy = electron −1 / null-identity 0 /
positron +1), proved choice-free — and it is BLIND, by construction, to the continuum MAGNITUDE (137.036), because
that magnitude lives in the Weyl/shape/Gibbs frame a counting machine has no channel for. That blindness is the
theorem, not the gap** — the category-error fence working (a count yields the identity + three states, never a
continuum). *NOT* "derives the structure, reserves the magnitude (incomplete)."

**The built spine every book draws on (choice-free where marked):** 0a (Formalization repaired) · 0b
`rank_heartbeat_connection = []` · 0c three blind readings (open=flat / loop=quantized division needle,
`loopRem`/`divStep` `[]` / accelerated=Gibbs-rejected) · capstone `one_eq_point_nine_repeating_count_to_three = []`
· Phase 1 `verify = []`. The anti-crank credibility: the −1/137 refused, the GR/QED endpoints reserved, the
accelerated ring rejected — three refusals, no number ever fished.

## Gauge 1 — Vol 1 · Measurement (the MATHEMATICAL register: math-without-math)
- **What it is (SPEC §3):** the construction from the first difference; *defines* tange/funge; names the electron;
  the α apex.
- **BUILT it draws on:** the whole spike; the existing fresh Vol 1 (24 ch / 6 acts / 145pp, [[vol1-measurement-fresh-build]]).
- **NEW / re-aim:** the apex CHANGES — from "stake the split, RESERVE the constant" to **the STRUCTURAL α**:
  `1 = 0.999…` as the naming identity, count-to-3 as the pigeonhole, the ±1 = (−i)² loop holonomy, and the
  Ricci-sees-structure / Weyl-blind-to-magnitude category boundary as the honest close. The relabeling-is-angular-
  momentum (Noether) grounding. One reworked apex act, not a rebuild.

## Gauge 2 — Vol 2 · Experimentation (the PHYSICAL register: physics without computation)
- **What it is:** the experiments, the constants, the courtroom — physics as physics.
- **BUILT:** the existing Vol 2 (30 ch / 360pp, done, [[experimentation-book-vol2]]).
- **NEW / re-aim:** α as the physical reading — **Thomas precession → g−2 → α**, the Unruh temperature (the earned
  cross-setup invariance), the Rindler frame, the matter/antimatter split as the three holonomy states, and the
  honest capability fence (the device prices α but **cannot detect a gravitational wave** — Ricci not Weyl). The
  Ch30 courtroom coda lands the verdict on the STRUCTURAL reading. Re-aim, not rewrite.

## Gauge 3 — Vol 3 · The Compiler (the COMPUTATIONAL register: computation without physics)
- **What it is:** the self-hosting compiler; the code as pure computation.
- **BUILT:** the existing Vol 3 (17 ch / 86pp, done, [[compiler-book-vol3]]) + the α spike as pure computation.
- **NEW / re-aim:** α as the COMPUTATIONAL reading — the fold as `spin`, the **division-algorithm needle**
  (`loopRem`, axiom-free), the **elaboration-heartbeat measurement** (deterministic → repeatable), the bisection,
  and the `verify` metaprogram. "1 = 0.999…" as the compiler identifying two representations (`selection_sound`).
  Re-aim around the reading.

## Gauge 4 — Vol 4 · Scientific Computation (the READING register: the code walk, all three unified) — NEW
- **What it is:** the user guide to the instrument; **NO tange/funge** — the distinction carried as citation
  texture (cited = funge/standard; uncited = tange/novel). The four explanations sharing no vocabulary = the
  null basis; their agreement + the code = the anti-crank evidence.
- **BUILT:** the whole instrument (`verify`, the α readings, the four explanations of `alpha_fold_probe_design.md`).
- **NEW:** the entire book (`books/scientific_computation/vol4_spec.md`). The reader re-runs `verify` and checks α
  themselves — repeatability IS the honesty. The headline chapter: *why the machine is blind to the number, and
  why that blindness is the result.*

## The null-basis check (why four gauges, not one)
Vols 1–3 explain the SAME α reading stripped to three non-overlapping registers (math / physics / computation) so
their agreement can't be a shared-word artifact; Vol 4 drops the words entirely (citation texture) and walks the
code. **Agreement across the null basis + the choice-free build = the anti-crank evidence.** Ship the number
without the gauges and you've shipped a reading nobody can interpret; ship the gauges and the reader verifies it.

## What this plan is NOT
Not the restructure. No `.tex` touched. Per-book: register + built-vs-new + re-aim, for Kodo's gate and the
operator's framing steer (the headline is the product's, so it's the operator's call). On green-light, Phase 2
executes book-by-book, gated.
