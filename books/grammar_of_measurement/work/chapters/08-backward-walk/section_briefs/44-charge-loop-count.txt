Section manager brief: beat 44, Charge is the loop count
========================================================

Beat:
- Charge is the tally of loop traversal.

Writer fragments:
- Fragment A: define loop count as the number of completed returns through the tower, not the length of
  an open path.
- Fragment B: explain billing: an open path may transport a distinction, but only a closed loop can charge
  the ledger because only closure returns the sign to the reference.
- Fragment C: tie charge to the seam: pass n closes; pass n+1 opens on residue; the tape appends rather
  than rewriting itself.
- Fragment D: use topological integer count and Aharonov-Bohm as examples only: integer winding and phase
  holonomy illustrate why closure, not local segment length, counts charge.

Integration notes:
- Keep the spine grammatical. The examples must not become the argument.
- Distinguish charge count from magnitude and from mass.
