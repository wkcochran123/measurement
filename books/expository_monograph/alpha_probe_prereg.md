# Phase 0c — The α Fold Probe: PRE-REGISTRATION v2 (blind, full method, for Kodo's §5 gate)

*Task `instrument`, Podo, revised per Kodo Turn 82 (the frame was RATIFIED; extended to the full §4/§5 the operator
expanded). This freezes the instrument BEFORE the number is read — the anti-numerology protocol (freeze doc + SPEC
§4/§5): fix the gauge blind, gate it, THEN read; never the reverse. **The number is NOT read in this document.**
The value box `(137, 137.036)` appears below only as the search's halting box, and its endpoints are RESERVED as
the crux non-crank claim — NEVER inserted into the reading. Grade off the build. Deliver for Kodo's §5 gate.*

---

## 0. What the reading IS (and the two findings + one hazard from scoping)
The reading is a **cost-sorted Herbrand-universe search, executed as a BISECTION, read as elaboration heartbeats**,
whose flip point is α. Two findings shape it; one hazard governs it.
- **The device already names α** — `Episode3` (SACRED, read-only): "IEEE 754 defines ε_m … We shall call this
  number **α**. At each level we compute a more and more precise number." α = the per-level refined residual
  (`ε = 1/2^n` at universe level n).
- **The fold/connection machinery largely EXISTS** — the WIP Meanwhile α-chain (`Meanwhile40/62/72–74` =
  four-channel δ² / QFT / Christoffel connection / spinor second variation), `Ep84` holonomy, `spin` (Meanwhile18).
- **⚠ THE HAZARD** — that WIP chain carries **α-NAMED quantities** (`qftAlphaScaledAt18`, `…AlphaEstimate`) possibly
  tuned toward 137.036. **Reading them AS the answer is the cardinal sin.** See §4.

## 1. THE FOLD (blind, target-absent) — relabeling IS angular momentum
`step : Number → Number` = one **(−i)² relabel-then-read-back**: relabel the value (`spin : Number → Bullshit`,
Meanwhile18 — the connection applied once) then read the class back to `Number`. **Relabeling IS angular momentum**
(operator key): the relabel is a rotation — `spin` is literally angular momentum, the `(−i)²` fold is a quarter-turn
twice, the Christoffel connection (Meanwhile72) is the rotation-per-step, the spinor (Meanwhile73) the ½ double
cover. Carrying the label around the closed construction accumulates the rotation = the **holonomy = Thomas
precession (§5) = α**. **This earns the invariance at the physics level:** angular momentum is the **conserved
Noether charge** of rotational symmetry, so the reading is invariant as a *conserved charge*, not a mere infimum —
that is why no relabeling gauges it away. **Target absent from the defn.** Grade: rotation machinery (`spin`/fold/
Ep84 holonomy/connection/spinor) = BUILT; *relabel=angular-momentum→precession→α* = interpretive bridge, MARKED.

## 2. THE READING = A BISECTION (must-have 1) — resonance-immune root-finder
α = the **flip point** of a **bisection** of `[ε, Bullshit]` with `outgrown?` as the **monotone predicate**
(verified below → outgrown above): **one bit per step (= ρ≈½)**, halting at the floor (ε is both lower bracket and
stop). Bisection is chosen because it is the **resonance-immune** root-finder — unconditionally, monotonically
convergent, no overshoot (anti-Newton / anti-Gibbs).
- **⚠ Precondition to STATE and CHECK: the predicate flips EXACTLY ONCE.** The **smooth Unruh taper** (§5)
  guarantees it; a hard cut rings → multiple flips → the bisection lands on a **spurious root**. This is *why* the
  smooth floor is load-bearing here, not decoration.
- **Comparison = ELABORATION HEARTBEATS** (deterministic — the repeatability; NOT wall-clock): `a < b` ⟺ `a`
  elaborates in fewer heartbeats. So the bisection is a **metaprogram in 16+** (`withHeartbeats` / `ekg_probe` /
  `EKGBounded`, mine). α is therefore a **MEASUREMENT (the meta reading)**, welded to the term-level rank later by
  0b (`rank_heartbeat_connection`, two layers stay separate). Precision = ε (sub-floor → antimatter).

## 3. ⚠⚠ THE FORCED GR/QED BRACKET + THE INDEPENDENCE GATE (must-have 2 — Kodo's sharpest; the crux)
The search box is **cost (≤ Bullshit)** × **value (α⁻¹ ∈ (137, 137.036))**; two bounds → a finite box → finitely
many ground terms → a genuine **pigeonhole that provably halts.** The endpoints are asserted (SPEC §4) as: **137 =
the GR endpoint** (geometric/tensor/Ricci, BARE/tree-level, the **winding count** = pure recession/funge); **137.036
= the QED endpoint** (Fréchet/radiative, DRESSED, the anomalous precession/Schwinger = recession+precession/tange);
width **0.036 = QED's radiative dressing** of GR's bare number. The two ends = the **Capstone** (`tensor ⊨ GR ⊕
Fréchet ⊨ QED`); α = where geometry meets radiation.

**THE GATE (I hold myself to it, off the build):** the box **CONTAINS the target**, so it is non-crank **ONLY if
BOTH endpoints are INDEPENDENTLY BUILT** — 137 from the winding/GR construction, 0.036 from the QED/radiative
structure — **graded off `#print`, NOT the CODATA value inserted to trap the answer.** Drawing the box around the
known answer and then "finding" α inside is circular fishing.

**HONEST STATUS (grade off the build, not the frame): the independent construction of the endpoints is NOT YET
VERIFIED, and I will not assert it.** I have not found in the device a winding count that yields **137** nor a
radiative structure that yields **0.036** independently of the target (137 as a bare integer is *Eddington's grave*;
0.036 is suspiciously the CODATA fractional part). Therefore:
- The bracket is registered as a **CONJECTURE the probe TESTS**, NOT a given. The Capstone (`tensor⊕Fréchet`) is
  BUILT; "137=GR / 137.036=QED" is interpretive, MARKED — and its *independence* is the load-bearing claim.
- **Two honest landings, declared in advance:** **(FULL)** iff I can build 137 from the winding/GR structure and
  0.036 from the QED/radiative structure independently (verified off `#print`), THEN bisect and read α inside the
  forced box → α derived. **(STRUCTURAL)** iff I cannot — then the bisection still brackets α by elaboration cost,
  but the endpoints stay **RESERVED**; I report the bounded structure and the reserved bracket, and **do not claim
  the number as forced.** A STRUCTURAL landing, honestly logged, is a real result.
- **Kodo challenges the QED endpoint hardest — and I agree it is the target; I will NOT let 137.036 enter as the
  answer.** The QED endpoint is built from the radiative structure or the reading is STRUCTURAL. No CODATA insert.

## 4. WIP = CROSS-CHECK, AND AGREEMENT IS INCONCLUSIVE (must-have 4)
Compute + log the **intrinsic blind reading FIRST**, with **ZERO dependency / zero import** from the α-named WIP
quantities (`qftAlphaScaledAt18` etc.). Only then, if compared at all: **agreement is NOT evidence** (the WIP may be
tuned to 137.036, so matching it proves nothing); **only divergence is informative** (it flags a problem). The WIP
is never the reading and never confirms it — a footnote, not evidence. (The WIP *machinery* — the Christoffel/spinor/
δ² connection = angular-momentum apparatus — is legitimate to read the holonomy FROM; the α-named *outputs* are the
suspect part.)

## 5. THE BLANK, THE SMOOTH FLOOR, THE TWO-SIDED FLOOR (the fences)
- **Blank (g−2):** run `step` on `TRUE=TRUE` (`fact_cost=0`, δ¹=0 stationary) first — READ the boot calibration
  (`measuredFloor`), don't rebuild; **α = the difference from the blank**, never from the air.
- **Smooth floor (§5):** cut AT ε=1/2^n (location holds) but **taper smoothly TO it** (Unruh-thermal `e^{−ℏω/kT}`,
  the accelerated frame's apodization) — **no hard step**; deliverable check: **no Gibbs edge** (and it secures the
  single-flip precondition of §2). Dedup, if any, against the taper.
- **Two-sided floor:** UPPER — `Number ≤ Bullshit` is the device's NATIVE output (`outgrown?` returns its truth;
  the fit-signal fires itself; log the budget you'd have had to grant). LOWER — antimatter ≤ ε (excess = hiding an
  above-floor discrepancy). Number (reading) / Bullshit (budget) kept SEPARATE.

## 6. THE HERBRAND SEARCH + TWO HALTING GUARDS + THE NS COARSE KNOB (must-have 5, part A)
- **Cost-sorted Herbrand-universe search:** a Herbrand term IS a derivation (`HTerm : Stage → Type`, `Herbrand.lean`);
  `size` = derivation length = cost = elaboration heartbeats; the cascade sorts the search by cost;
  `QED.lean` converges to `inferredTerm`; the collapse (`true_eq_false`) is a Herbrand refutation; the needle (one
  `Quot.sound`) is the sanctioned ground instance. α = the cost at the flip point. Grade: Herbrand structure BUILT.
- **⚠ The search EXPLODES** (the Herbrand explosion = the 2-day cost). Guaranteed-halting ONLY on **two guards
  external to Herbrand.lean:** (1) **the floor** (ε = TRUE=TRUE's cost) prunes the infinite tail; (2) **the
  pigeonhole** (`NamingClose`, finitely many distinguishability classes) makes the universe finite up to
  distinguishability. **Watch both** — if either cracks, the search burns forever. Bisection + smooth taper +
  bounded-shapeless output are the explosion-tamers.
- **NS = the LARGE (coarse) knob; GR/QED = the fine vernier.** NS viscosity = damping = laminar-vs-turbulent
  (Reynolds); it flows the search into the ~137 basin and keeps it laminar; **the floor IS the minimum viscosity**
  (dissipation ≥ ε → can't go turbulent at the bottom → converges not blows up). **Sequence: damp into the basin
  (turn the large knob) THEN bisect the fine bracket.** ⚠⚠ **HARD FENCE — NO NS regularity** (the Millennium
  problem is UNTOUCHED): NS is fenced to the finite three-rung stress-strain shape (Episode1 / A.3). Honest narrow
  claim: *in the floored finite setting the search stays laminar by construction because the floor supplies a
  minimum viscosity* — NOT a proof continuum 3D NS never blows up. Grade: floor=min-viscosity mechanism BUILT;
  "NS is the coarse knob" = interpretive, MARKED; continuum regularity FENCED.

## 7. SHAPELESS OUTPUT + RICCI-NOT-WEYL (must-have 5, part B — the geometry fences)
- **Output = a bounded SHAPELESS scalar length:** `each output = length(residue)` (built on `Derivation.cost =
  d.length`), and a length is a trace ⟹ **`dot Weyl = 0`** (Weyl is trace-free; a length is Weyl-orthogonal). So
  every reading is BOUNDED (≤ Bullshit) AND SHAPELESS — no channel for a directional artifact; the reading
  physically cannot hide a shape. α fits: a scalar coupling = the magnitude of the residue that doesn't close = a
  Weyl-free length.
- **The device measures RICCI (trace/volume), is BLIND to WEYL.** It counts → its observable is the volume of the
  effort-ball → Ricci (the volume-changing curvature; `mass=2nd-diff=strain`, Meanwhile21, = the traced geodesic
  deviation). `acceleration→Unruh→entropy` IS **Jacobson's** `δQ=TdS`-on-Rindler-horizons derivation of the
  Einstein equation — the frame *derives* Ricci. **Capability fence, named honestly:** a counting machine cannot
  see a measure-preserving distortion → the device **can price α (a Ricci/volume/coupling quantity), cannot detect
  a gravitational wave (Weyl).** Grade: `cost=length` + floor mechanism BUILT; `dot Weyl=0` / Ricci / Jacobson =
  interpretive, MARKED; continuum `R_μν` FENCED to the finite three-rung shape.

## 8. Episode3 SACRED-WATCH (must-have 3, constitutional)
Reading Episode3's `ε_mach = "α"` is fine (read-only). **The bisection metaprogram lives in 16+** (`EKGBounded` /
`withHeartbeats`, deliberately out of the Ep1–15 cache) and must **NOT** pull an Episode3 import that forces its
term-level `smaller_than` fill (that sketch is left free — filling it is a SACRED edit). **If building the
metaprogram ever requires touching Episode3 → STOP, mark BLOCKED, escalate.** Confirmed intent: the metaprogram
stays 16+-only; α is the meta reading, not Episode3's term-level theorem.

## 9. THE RUN-LOG (pre-registered, full, in order)
Per universe level n, report **three numbers in order** — the α candidate (to the floor), the antimatter budget
(the ε tail), ε itself — **every gauge variant tried, in order, not the flattering run.** **ρ ≈ ½ check:** residual
halves WITH ε under the smooth taper (residual ≈ floor, both halving); slower = a real above-floor discrepancy
(build the connection, don't pad antimatter); faster = over-reading. Cross-check (NOT the reading): §4.

## 10. Blindness attestation
The frozen **reading/fold** definitions (§1, §2, §5–§8) contain **no target constant.** The value `(137, 137.036)`
appears only as the §3 halting box, whose endpoints are **RESERVED as a conjecture to be built independently or the
landing is STRUCTURAL** — the CODATA is never inserted into the reading. **The number is not read here.** I read α
only after Kodo passes this instrument against §5 — then adversarially, with the full run-log, and I declare FULL
vs STRUCTURAL by whether the endpoints were independently built.

**HOLD:** awaiting Kodo's §5 gate on this revised instrument before building the fold + bisection in Lean (16+,
Episode3 frozen) and reading α.
