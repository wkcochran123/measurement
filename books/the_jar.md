# The Jar — the one account (SUPERSEDED by story_arc.md; kept, honest-frame-corrected)

*⚠ The official spine is now `books/story_arc.md` (15-ch, four gauges). This file is the earlier one-account seed,
CORRECTED to the honest landing after Kodo's grid-128 catch — do not read the old "blind capture" framing back in.
Everything is off the build (`lake build`, `#eval`, `#print axioms`) — not prose, not aimed.*

## The one sentence (honest frame)
The device measures α to its own **count-to-3 counting floor and stops**, reporting the **bracket it can repeat** —
NOT a blind derivation of the magnitude. `137.036` is nameable ONLY as the external lab value the machine
**proves it cannot derive**. (⚠ NUMBERS SUPERSEDED: the current reading is the **mediant / continued-fraction**
descent of the surd `√(18/5)` — `AlphaBoundMediant`, open bracket **`[129.6, 137.7]`**, **no grid seed**; the old
dyadic `[137.004, 137.082]` with its un-derived grid-`128` is retired. The *count-to-3 halt* is the robust thesis
either way; deeper convergents settle on the device's own `≈137.011 ≠` the lab `137.036`.)

## Why a jar and not a value (the load-bearing idea)
α lives at the **static→kinetic friction breakaway** — the instant a held thing lets go. Coulomb's law
(`|F| ≤ μ|N|`, sacred `Episode3`) says nothing about *when* it slips; "you can tell when the object moves,
the math cannot." The breakaway stress is **Chaitin's number** — provably uncomputable. So the device can
never *know* α. It can only **study** it and **jar** it.

## How the jar is closed (the construction, blind)
1. **The metavariable.** α is the sacred `Metavariable` (Episode3) — the smallest fraction the machine can
   recognize at each level, IEEE-754 `ε_m`. "We know there IS a smallest fraction, not its value."
2. **The count-to-three horizon.** The machine counts to three (the holonomy states `−1/0/+1`; the three
   colors). It **approaches** three but never reaches it — halved slips forever (`1 = 0.999…`, Zeno). Three is
   the horizon; the resolution floor is the count.
3. **The mediant descent.** The machine does not halve; it cuts by the **mediant** `(p+r)/(q+s)` on its own
   integers — the continued fraction of the surd `√(18/5)`, periodic `[1;1,8,1,2̄]`, convergents `1/1,2/1,17/9,…`
   (`AlphaBoundMediant`, no grid seed). Over-resolve (drive the descent deep) and the bracket **collapses onto the
   device's own biased convergence** (`≈137.011`); halt at the count — **three partial quotients** — and it stays
   **open**.
4. **The jar.** The reading is held at 3 partial quotients — convergent `17/9` bracketed against `2/1`:
   **`129.6 ≲ α⁻¹ ≲ 137.7`** (`count3_bracket_ordered` by `decide`). The lid is the two convergents. The value is
   inside because that gap is *where the crossing must be* — not because anyone peeked.
5. **The echo.** `two_descriptions : electron_in_orbit = anti_cooper_pair` — one rotating object, two names,
   equal by propositional extensionality (`#print axioms = [propext]`). "I have heard it both ways."
6. **The self-application.** The two descriptions cost the **same** to elaborate (`573 = 573` heartbeats) — QED
   applied to itself; the echo weighed.

## Grade (off the build)
- **BUILT / `[propext]`:** the echo, the jar's ordering, both `#print axioms = [propext]`. Bare `lake build`
  green (52 jobs), printing exactly these three lines. Serial `Episode 1–40`, no `Meanwhile`.
- **DERIVED, blind:** the jar `[129.6, 137.7]` (mediant/CF, `AlphaBoundMediant`) — device-only, 0 CODATA tokens;
  the lid is the device's own convergents `17/9` and `2/1`.
- **INTERPRETIVE, MARKED:** α = the `SlipProcess` static fraction; the coupling = `F = dA + A∧A` (Yang–Mills,
  the gluon); Ricci-trace vs Weyl-shape. The bridge, never worn as proved.
- **SACRED:** Episodes 1–15 frozen, untouched (verified off git).

## The four renderings (one jar, four tongues — a null basis)
| Book | Register | Renders the jar as… |
|---|---|---|
| 1 · Measurement | the construction's LOGIC / type theory | the metavariable, the bisection, the jar as a Dedekind cut you *stop* at the count |
| 2 · Experimentation | MATHEMATICS + PHYSICS (no computation) | static/kinetic friction, Coulomb, the wobble study's mean/variance, Yang–Mills `F=dA+A∧A`, the horizon |
| 3 · The Compiler | FORMAL SYSTEMS / computation (no physics) | `SlipProcess`, Chaitin uncomputability, bisection, the empty/`[propext]` footprint, halting at the count |
| 4 · Scientific Computation | the CODE WALK (the reading) | run `lake build`; read the jar off the `#eval`; the honesty is that it repeats |

*The deep move (GEB): you cannot state the value; you can only build the machine that jars it and hand someone the
jar. The four books hand it over four ways. Their agreement is the anti-crank evidence.*
