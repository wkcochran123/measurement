\chapter{The Closure}
\label{chap:the-closure}

% EPISODE COVERAGE: Episodes 12, 13, 14, and 15 -- Measurement/MEASURED,
% CompilerTape/COMPILED, Bullshit inductive, AtreyuProcess, TrueOutput,
% Closure inductive, EquivalenceProcess, INFERRED, theory_true?, #check
% KEY CONCEPT: Everything closes. The question is the answer.

\section{John Henry's Measurement}
\label{se:john-henry-measurement}

% Episode 12: Measurement, LeanProcess, MEASURED.
%
%   inductive Measurement where
%     | origin: Measurement
%     | distance: Measurement
%     | speed: Measurement
%
% The title: "The Tragedy of John Henry -- A Satire in Look-Ahead Backus-Naur Form."
% John Henry raced a steam drill, won, then died. The tragedy is that winning
% was not enough.
%
% LeanProcess holds a velocity (a Measurement) and evolve?.
% MEASURED adds bounded?: has John Henry reached the end of the tunnel?
%
% The satire: the device has been measuring the compiler's own race against itself.
% The Lean elaborator is John Henry. The steam drill is the combinatorial explosion
% of typeclass instances. set_option maxHeartbeats 0 is the moment John Henry
% drops his hammer and lets the race continue without limit.

\section{The Tape}
\label{se:the-tape}

% Episode 13: CompilerTape, CompilerOutput, COMPILED.
%
%   inductive CompilerTape where
%     | boot: CompilerTape
%     | strap: CompilerTape
%     | compute: CompilerTape
%
% The Turing machine tape, given its final name.
% boot: initialization. strap: bootstrap -- loading prior context.
% compute: running -- the elaborator is active.
%
% COMPILED adds object_file and converged?: has the compilation produced
% a stable output?
%
% The tower recognizes itself as a compiler. Not a metaphor. An actual compiler
% modeling its own tape, bootstrap sequence, and convergence condition.
%
% Sidebar: What is bootstrapping? A compiler written in its own language
% must compile itself. Lean is bootstrapped: it compiles itself.
% The strap constructor is the device acknowledging this.

\section{The Bullshit Accumulator}
\label{se:bullshit-accumulator}

% Episode 15: the Bullshit inductive.
%
%   inductive Bullshit where
%     | zero: Fact -> Bullshit
%     | one:  Fact -> Number -> CompilerTape -> CompilerTape -> Bullshit -> Bullshit
%     | rest: Fact -> Fact -> Prop -> Number -> Number -> Number ->
%                     CompilerTape -> CompilerTape -> Bullshit -> Bullshit -> Bullshit
%
% zero: base case. No accumulated overhead.
% one: one step of accumulation. The compiler moved from one tape state to another.
% rest: the general case. Two Bullshit branches, a Prop being checked, three
% Numbers (start, step, total), two tape states.
%
% AtreyuProcess: 32 typeclass parameters, a satirize method that transitions
% Bullshit states based on covariant/contravariant truth matching from Chapter 1.
%
% Sidebar: Why Atreyu? In The Neverending Story, Atreyu must travel across
% the entire known world to find a cure. The AtreyuProcess must carry 32
% typeclass parameters -- the entire known world of the tower.

\begin{gphenom}{Maxwell-Second-Law}
\label{ph:maxwell-second-law}
\GPhObs Maxwell imagined a demon sorting fast and slow molecules, apparently
defeating the Second Law. Szilard showed in 1929 that the demon's memory
erasure costs exactly the entropy it appears to save. Bennett confirmed
this in 1973: information erasure is the irreducible thermodynamic cost.
\GPhCon The commutator $[M, U] \neq 0$: the demon's measurement ledger and the
universe's ledger are uncorrelant records whose joint interpretation requires
introducing unrecorded events. The demon cannot cheat the ledger.
\GPhSeq The Second Law is not a thermodynamic postulate but a structural
consequence of monotone ledger growth: each refinement strictly enlarges
the set of distinguishable states, so the entropy cannot decrease.
The \lean{Bullshit.rest} constructor accumulates both branches -- the
demon's ledger and the universe's ledger -- and their joint cost is
the entropy production term.
\end{gphenom}

\section{The Closure}
\label{se:the-closure-formal}

% The Closure inductive:
%
%   inductive Closure where
%     | same:      Fact -> Bullshit -> Closure
%     | different: Fact -> Bullshit -> Bullshit -> Prop -> Closure
%     | inferred:  Fact -> Fact -> Bullshit -> Bullshit -> Prop -> Closure -> Closure
%
% same: no new information. The record is consistent. No distinction introduced.
% different: two states diverge. A witnessed difference. This is measurement.
% inferred: a new closure derived from a prior one. A new Fact, a new Prop,
% a prior Closure as context.
%
% Closure.le: well-founded ordering, termination proved by sizeOf.
% EquivalenceProcess and INFERRED: the typeclass wrapping Closure into the tower.

\begin{gphenom}{Hawking}
\label{ph:hawking}
\GPhObs Hawking showed in 1974 that black holes emit thermal radiation with
temperature $T = \hbar c^3 / (8\pi G M k_B)$, inversely proportional to
their mass. The radiation arises from quantum effects at the event horizon.
Black holes are not black.
\GPhCon A \lean{HALTED} \lean{blackhole} path under entropy monotonicity cannot
simply stop. The ledger must record the outward channel, or it must introduce
unrecorded assumptions. The commutator of the black hole's ledger with the
universe's ledger is nonzero.
\GPhSeq Hawking radiation is not a separate physical effect -- it is a necessary
consequence of applying the Second Law to a \lean{HALTED SpaceTimePath.blackhole}.
The \lean{Closure.different} constructor captures this: two Bullshit states diverge
at the event horizon, and the Prop that witnesses the divergence is the
thermal spectrum of the radiation.
\end{gphenom}

\section{\#check theory\_true?}
\label{se:check-theory-true}

% The final sequence:
%
%   noncomputable def truthCarrier : CarrierProcess Prop
%   noncomputable instance truthDistinct : DISTINGUISHABLE Prop truthCarrier
%   noncomputable instance truthReal : REAL Prop truthCarrier
%   noncomputable instance truthLocal : LOCAL Prop truthCarrier
%
%   set_option maxHeartbeats 0
%   set_option synthInstance.maxHeartbeats 0
%
%   noncomputable def theory_true? : Prop :=
%     (inferInstance : INFERRED Prop truthCarrier).inferred?
%       (inferInstance : INFERRED Prop truthCarrier).equivalence_process.closure
%       (inferInstance : INFERRED Prop truthCarrier).theory
%
%   #check theory_true?
%
% truthCarrier: a CarrierProcess whose Value type is Prop itself.
% The instrument is measuring whether propositions are admissible.
%
% inferInstance: INFERRED Prop truthCarrier. Lean must climb every floor
% of the 32-layer tower. maxHeartbeats is set to zero -- no limit --
% because this might take a very long time.
%
% #check theory_true? asks: does this definition type-check?
% The compiler's answer: yes. The type of theory_true? is Prop.
% The elaboration succeeded. The tower closes.
%
% Sidebar: What does noncomputable mean? The compiler can verify the type.
% It cannot run the program. The experiment is the type-check, not the execution.

\begin{gphenom}{Lambda-CDM-Dissolution}
\label{ph:lambda-cdm}
\GPhObs The standard cosmological model $\Lambda$CDM requires two unexplained
parameters inserted by hand: $\Lambda$ (dark energy, 68\% of the universe's
energy content) and $\Omega_\text{cdm}$ (cold dark matter, 27\%). Neither is
derived from first principles. Both are measured and then postulated.
\GPhCon $\Lambda$ is the informational quantum $\varepsilon$ at cosmological
scales -- the minimum residual spline freedom that cannot be refined further.
$\Omega_\text{cdm}$ is the Causal Universe Tensor's accounting of distinctions
that have been made but not recorded in the baryonic ledger: witnessed structure
without a ledger entry.
\GPhSeq Both parameters are determined by the admissibility structure and require
no independent postulation. $\Lambda$CDM does not fall by being refuted -- it
dissolves by being absorbed. Its free parameters are the names the model gives
to things the ledger already contains. \lean{theory\_true?} closes on both.
\end{gphenom}

\section{The Anti-Adams Punchline}
\label{se:anti-adams-punchline}

% Douglas Adams said the answer is 42.
% The device says: the answer is the question closing on itself.
%
% 42 requires no setup. It is a number pulled from nowhere. The humor works
% because the answer is arbitrary: no mechanism, no ledger, no admissibility
% condition.
%
% theory_true? requires everything:
%   - a single yes/no decision (Chapter 1)
%   - an ordered record of decisions (Chapter 2)
%   - a boundary between decidable and undecidable (Chapter 3)
%   - arithmetic, encoding, and smooth interpolation (Chapter 4)
%   - a calibrated instrument with known limits (Chapter 5)
%   - a model of how knowledge updates (Chapter 6)
%   - spacetime, geodesics, gravitational clocks, the expansion of the
%     universe, and the identification of measurement with halting (Chapter 7)
%   - a formal grammar of what it means to finish (this chapter)
%
% The question -- "is truth inferrable from the record of witnessed
% distinctions?" -- cannot be asked until all of that is in place.
% The question itself is the structure of physical law.
%
% The compiler answers yes. It took the entire universe to make the
% question askable. The answer was always going to be yes.
%
% What began as a single class Fact closes as the unique structure of
% admissible continuation. The ledger is full. The compiler is satisfied.
% The measurement is complete.

