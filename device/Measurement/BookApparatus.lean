/-
BookApparatus -- the EXPOSITION tree (story_arc fidelity). NOT the convergence: the root
`Measurement.lean` stays the minimal 3-line convergence (bare `lake build`, 52 jobs). The books
grade their apparatus/field/Dedekind claims against THIS target. Two trees, by design.
Photoelectric is NOT included: un-atticking it drags Float+over-resolution (CooperManual, the
Meanwhile41-62 chain, Mott/Stern-Gerlach) -- proposed DROP (the in-build slip minimum is the
static->kinetic analog). Everything here is grep-confirmed for the arc's Ch5/8/10/11/12/13/14.
-/
import Measurement.Episode40        -- the slip chain: Cavendish, orbit, Lorentz, Dirac, Galerkin, proximity, E29/35/40, the alpha formula
import Measurement.AlphaDedekind    -- Ch13.2: the Dedekind cut, bound = 60 (uses RichardsonSelfNaming = the self-energy)
import Measurement.NamingClose      -- Ch8: the naming / pigeonhole
import Measurement.SelfApplication  -- Ch14: self-application, withHeartbeats, rank<->heartbeat
import Measurement.TwoDescriptions  -- the echo (two_descriptions = ...)
import Measurement.AlphaBoundCountToThree  -- Ch13/15: the count-to-3 bracket (the honest jar)
